# MedKit 三报告整合 · 优化清单与执行记录（2026-09-15）

> **用途**：把同日产出的三份报告去重合并为**单一优先级清单**，并作为本轮改进的**执行记录**（边执行边回填）。
> **输入（三份）**
> 1. `docs/工程审查改进指南_2026-09-15.md`（R6 指南，六维工程品质，R6-01~R6-22）
> 2. `docs/reviews/ux-walkthrough-medstudent-2026-09-15.md`（UX 走查，考前医学生视角，A1~A5 / B1~B5 / P0-1~P3-18）
> 3. `docs/reviews/工程质量独立核查报告_2026-09-15.md`（独立核查，仓库事故 + 五维复核，B-1~B-3 / H-1~H-6 / M-1~M-7）
>
> **整合口径**：三份报告存在大量**同源问题**（如「async 阻塞」同时出现在 R6-03 与核查 H-1）。
> 本清单统一编号为 **U-xx**，每项标注 `来源` 交叉引用，**只保留一份改进动作**，避免重复施工。
> **优先级口径**：`P0` = 功能不可用/不可见 或 已发布产物含数据安全缺口；`P1` = 违反已立规矩或影响正确性/性能量级；
> `P2` = 规范缺位/体验缺陷；`P3` = 打磨。
> **证据口径**：全部结论以命令实测（ruff / pytest / AST / grep / git），不转述文档。

---

## 1. 现状核验（本轮开工前实测，2026-09-15 12:1x）

| 项 | 实测 | 结论 |
|---|---|---|
| `HEAD` | `a70df12`（12:08） | 已含 R6-01 / R6-11 / R6-15 三批提交 |
| 领先 `origin/master` | **0** | 前序会话已完成恢复并推送（核查报告 §1.4 收口动作已执行） |
| `ruff check .` | **All checks passed** | ✅ 批次 1 已闭环 |
| 未跟踪文件 | 3 项（本次三份报告 + 走查截图） | ⏳ 待入库（U-04） |
| 版本号 | `0.10.1`（未 bump） | ⏳ U-01 待办 |
| `asyncio.to_thread`（routers） | **3** 处 | ⏳ U-05 待办 |
| `core → routers` 反向依赖 | **2** 处（`core/gap.py:200-201`） | ⏳ U-09 待办 |
| 上帝函数 ≥400 行 | **2** 个（`_run_project_impl` 699 / `export_paper_html` 439） | ⏳ U-10 待办 |
| `window.FEATURES` 误用 | **2** 处（`learn.js:1468,2226`） | ⏳ U-03 待办（记忆卡不可见） |
| `rvQueueAll` 缺 `method:POST` | **未修**（`learn.js:2205`） | ⏳ U-02 待办（铺卡 405） |
| `#qreset` 无 `data-label` | **未修**（`qbank_html.py:394` vs `setCounts()`） | ⏳ U-08 待办（显示 null） |
| 检索 Key 回退 | **未修**（`orchestrator.py:605` 仅用 `ws_cfg` 自身 Key） | ⏳ U-06 待办 |
| 错因缺省 | **未修**（`library.py:353` 缺省 `"reasoning"`） | ⏳ U-19 待办 |

> 已完成（本轮不再重复）：**R6-01**（总闸转绿）、**R6-11**（打包纯净检查入闸）、**R6-15**（README 版本口径）、
> **核查报告 B-3**（仓库事故恢复并推送）、**R6-12**（复核撤注，NX-06 本就合规）。

---

## 2. 整合优化清单（按优先级排序）

### P0 —— 功能不可用 / 发布缺口（必须先做）

| 编号 | 来源 | 改进内容 | 涉及模块 | 预期效果 | 验收标准 |
|---|---|---|---|---|---|
| **U-01** | R6-02 · 核查 B-1/B-2 | 版本 bump 至 **0.10.2**，使 R5 数据安全修复进入发布口径；旧 0.10.1 产物改名隔离 | `medkit/__init__.py` · `pack/version.iss` · `CHANGELOG.md` · `pack/release-notes.md` · `README.md` | 消除「同一版本号两个不同产物」，用户拿到的包含 P0 数据安全修复且版本可区分 | `python -c "import medkit;print(medkit.__version__)"` = `0.10.2`；`grep -c "0.10.1" CHANGELOG.md` 仅历史节 |
| **U-02** | UX A1（P0-1） | 前端「铺卡（薄弱点全部入队）」用 GET 调后端 POST 路由 → 补 `method:"POST"` | `medkit/web/js/learn.js:2205` | 恢复批量入队功能（实测 POST 后立即入队 11 张） | 单测断言全仓前端调用与后端路由方法一致；`pytest tests/browser` 无回归 |
| **U-03** | UX A2（P0-2） | `window.FEATURES` 判断失效（`app.js:10` 用 `const` 不挂 window）→ 改为 `FEATURES` | `medkit/web/js/learn.js:1468,2226` | 恢复「🧠 生成记忆卡」入口与记忆卡面板（v0.8.1 功能重新可达） | 断言 `FEATURES.cards===true` 时入口存在；`typeof window.FEATURES` 不再是判定条件 |
| **U-04** | R6-14 · 核查 §1.4 | 三份审查报告 + 走查截图入库提交 | `docs/reviews/*`（3 份 md + `ux-walkthrough-shots/`） | handoff/提交信息引用的报告不再 404；报告成为可追溯资产 | `git status --porcelain` 无 `??`；`git log` 含归档提交 |

### P1 —— 违反已立规矩 / 影响正确性与性能量级

| 编号 | 来源 | 改进内容 | 涉及模块 | 预期效果 | 验收标准 |
|---|---|---|---|---|---|
| **U-05** | R6-03 · 核查 H-1 | `async def` 端点内秒级~分钟级阻塞操作移入线程池 | `routers/syllabus.py`(224/168/234) · `library.py`(128) · `projects.py`(446) · `realexams.py`(57) | 导入大纲/素材期间事件循环不再被占满，全站不再冻结数分钟 | `grep -c "asyncio.to_thread" routers/*.py` ≥ 9；新增「检索中 health 仍 <500ms」并发用例 |
| **U-06** | UX A4（P1-4） | 生成管线检索 Key 解析对齐服务商 Key 回退（同 `search.py:62`） | `medkit/core/orchestrator.py:592,605` | 网络检索不再必然失败；不可用时生成前明确拦截而非跑完 3 轮写复核清单 | 单测：仅配服务商 Key 时 `resolve_backend` 得到可用 Key |
| **U-07** | UX A5（P1-5） | 查重（dup）类问题与 fail 同口径处置：剔除并计入人工复核清单，产物计数同步 | `medkit/core/orchestrator.py`（门禁①收口段） | 重复题不再进产物，押题卷不再出现同一题两遍 | 单测：构造 dup 组 → 产物中不含重复题且复核清单有条目 |
| **U-08** | UX A3（P1-3） | `setCounts()` 只遍历带 `data-t` 的按钮，避免 `#qreset` 被写成 `null` | `medkit/render/qbank_html.py:470-479` | 筛选区「重置」按钮正常显示中文，不再出现英文字面量 `null` | 产物渲染冒烟断言 `#qreset.textContent === '重置'` |
| **U-09** | R6-05 · 核查 H-3 | 消除 `core → routers` 反向依赖；SQL/迁移从路由层回归 core | `core/gap.py:200-201` · 新建 `core/projects.py` · `routers/projects.py:98` · `routers/syllabus.py:249-257` · `core/db.py` | 分层恢复单向，路由可独立测试，事务边界收敛 | `grep -rn "from ..routers" medkit/core/` 零命中；`grep -rn "cur.execute\|dbs.migrate()\|dbs.tx(" routers/*.py` 零命中 |
| **U-10** | R6-04 · 核查 H-2 | 拆 `_run_project_impl`(699) 为 8 个阶段函数 + `PipelineContext`；拆 `export_paper_html`(439) 为 4 片段函数 | `medkit/core/orchestrator.py:487` · `medkit/render/qbank_html.py:531` | 阶段可独立测试/回滚，消除并发类缺陷高发区 | AST 判据：无 ≥400 行函数；断点续跑/取消/子步骤事件全量复跑零回归 |
| **U-11** | R6-06 · 核查 H-4 | 立 ADR-006：JSON 轨退役条件、步骤、回滚、影响文件清单 | 新建 `docs/adr/ADR-006-retire-json-store.md` · `docs/README.md` | 双轨有明确终点，堵住「回落写真实路径」的结构性数据事故入口 | ADR 含四节；`docs/README.md` 新增 ADR-006 引用 |
| **U-12** | R6-07 · 核查 H-6 | 覆盖率门槛 + 防丢断言（断言实现结构而非行为）+ 「先红后绿」纪律入 handoff | `requirements-dev.txt` · `pyproject.toml` · `.github/workflows/ci.yml` · 新建 `tests/test_stream_wiring.py` · `docs/AGENT_HANDOFF.md` | 489 用例从「虚假信心」变为可度量信心 | `pytest --cov` 有数字；反向验证（移除 `dedupe.end` → 用例必须红） |
| **U-13** | R6-09 前端 · 核查 H-5 | `innerHTML` 收敛（156 处）统一走 `esc()`；产物页拼接点转义补强 | `medkit/web/js/*.js` · `medkit/render/qbank_html.py` | 收敛 XSS 面 | `grep -c innerHTML` 显著下降；转义断言通过 |

### P2 —— 规范缺位 / 体验缺陷

| 编号 | 来源 | 改进内容 | 涉及模块 | 预期效果 | 验收标准 |
|---|---|---|---|---|---|
| **U-14** | R6-08 · 核查 M-1 | 迁移 v7：为 v1 五张表补索引；列表读取按需解析 JSON | `medkit/core/db.py`（MIGRATIONS + `_V7_UP/_DOWN`） | 学习中心列表/掌握度/推荐不再全表扫描 + 全量 `json.loads` | 迁移测试含 v7 升降；`sqlite_master` 可见新索引 |
| **U-15** | R6-13 · 核查 M-5 | 异常治理：静默必留痕 + 错误计数 + 只读诊断入口 + 错误体不再回显模型原文 | 新建 `medkit/core/errors.py` · `medkit/state.py` · `main.py` · 前端「我的」 | 「按钮点了没反应」可自查；对外错误统一中文 + error_code | `grep "except Exception" -A1 | grep -c "pass$"` 从 31 降到 ≤5 |
| **U-16** | R6-10 · 核查 M-3 | 依赖治理：锁文件 + `pip-audit` + `dependabot` + `LICENSE` + 第三方许可证清单 | `requirements.lock` · `.github/workflows/ci.yml` · `.github/dependabot.yml` · `LICENSE` · `THIRD_PARTY_NOTICES.md` | 构建可复现、可审计、可回答许可证问题 | 五文件均存在；`pip check` 零输出 |
| **U-17** | R6-09 · 核查 M-2 | 前端工程化：ESLint（本地，零 CDN 不破）+ ES Module 化 + 按域拆分超大 JS | `package.json` · `.eslintrc.json` · `medkit/web/js/*.js` · `index.html` | 前端改动有静态防线；消灭「加载顺序即契约」 | `eslint --max-warnings 0`；最大 JS 文件 ≤800 行 |
| **U-18** | UX B1（P2-8） | 题库页头部题数与筛选计数口径统一（B1 组按子题计或显式说明） | `medkit/render/qbank_html.py` | 考生不再怀疑「是不是丢了 2 题」 | 产物断言：头部计数 == 筛选合计 |
| **U-19** | UX B2（P2-9） | 押题卷回流错题带真实错因；缺省值 `reasoning` → `unknown`，UI 显示「未标注」 | `medkit/core/library.py:353` · `medkit/render/qbank_html.py:892` | 错因不再恒为「推理断链」，不误导薄弱点判断 | 单测：不传 `error_reason` 时入库为 `unknown` |
| **U-20** | UX B3（P2-10） | 补「数据管理」区（路径/备份/占用/恢复/清空）或修正副标题 | `medkit/web/index.html:107` · `medkit/web/js/app.js` · 后端数据管理接口 | 兑现副标题承诺，用户可自助备份/清理 | 副标题与实现一致；数据管理入口可见 |
| **U-21** | R6-22 | 协程驱动机制两份实现统一到 `run_coro` fixture | `tests/test_r4_batch3.py:84` · `tests/conftest.py` | 消除「同一问题两种写法」的口径不一致 | `grep -rn "_run_async" tests/` 零命中 |

### P3 —— 打磨（可延后）

| 编号 | 来源 | 改进内容 |
|---|---|---|
| **U-22** | UX B4/B5 | 讲解重复生成提示覆盖/新建；脏数据不参与掌握率分母、下拉排除 |
| **U-23** | UX P3-12~18 | 首启四卡 2×2、跳过引导对比度、向导后引导条；测试连接进度/耗时；素材可选框折叠；审核台搜索框；年份空筛选隐藏；考试计划驱动行为 |
| **U-24** | R6-16~21 | 兼容 re-export 退役清单；mypy 配置；`medkit.spec` excludes 显式化 + 体积断言；`RedactingFilter` 日志脱敏；jieba/pkg_resources 定时炸弹跟踪 |

---

## 3. 执行顺序与依赖

```
P0（串行，先做）
  U-01 版本收口 ──┬─→ U-04 报告入库（同批提交）
                  └─→ 其余项（版本号是后续所有验收的口径基准）

P1（U-02/U-03/U-06/U-07/U-08 互不重叠文件，可并行）
  U-05 异步阻塞 ∥ U-09 分层 ∥ U-11 ADR ∥ U-12 测试可信度
  U-10 上帝函数（依赖 U-12 测试网就位后再动，高风险）
  U-13 innerHTML（依赖 U-17 前端工程化基线）

P2
  U-14 索引 ∥ U-15 异常 ∥ U-16 依赖 ∥ U-19 错因 ∥ U-18 计数
  U-17 前端工程化 → U-13
  U-20 数据管理 ∥ U-21 协程统一

P3
  U-22 ∥ U-23 ∥ U-24
```

**每批次总闸**
```bash
python -m ruff check .
python -m pytest -q --ignore=tests/browser     # 单测（与 browser 分进程）
python -m pytest tests/browser -q              # 浏览器层（可 SKIP_BROWSER=1）
python pack/check-package.py                   # R6-11 后为固定步骤
```

---

## 4. 执行记录（本轮回填）

> 每条记录：**改了什么（文件:行）· 验收命令 · 实测结果**。
> 提交：`db46fc9`（U 批次1）· `f9fa225`（U-01）· `0285ea7`（U-05/U-09）· `5e60773`（U-14/U-15/U-11）
> · `5c48f14`（U-16）· `ae98251`（U-12/U-21）· `6ec479c`（U-13）

### 4.1 P0 批次（功能不可用 / 发布缺口）

| 编号 | 改动 | 验收实测 |
|---|---|---|
| **U-01** | `medkit/__init__.py` / `pack/version.iss` / `CHANGELOG.md` / `pack/release-notes.md` / `README.md` 五件套统一到 **0.10.2**；`dist-installer/` 旧 0.10.1 两件套改名加 `-pre-r5` 后缀 | `python -c "import medkit;print(medkit.__version__)"` → `0.10.2`；`test_s2_refactor.py` 的单源一致性断言通过 |
| **U-02** | `learn.js` 的 `rvQueueAll` 补 `method:"POST"` | 新增前后端方法一致性守卫（全仓扫描前端 `api()`，POST-only 路由必须显式 method）；**反向验证**：回退后精确指向 `learn.js:2205` |
| **U-03** | `learn.js` 的 `window.FEATURES` → `FEATURES`（2 处） | 守卫断言全仓无 `window.FEATURES` |
| **U-04** | 三份报告 + 走查截图 + 本清单入库 | `git status --porcelain` 无 `??` |

### 4.2 P1 批次

| 编号 | 改动 | 验收实测 |
|---|---|---|
| **U-05** | `routers/syllabus.py`（4 处）/`library.py`（2）/`projects.py`（1）/`realexams.py`（1）阻塞操作改 `await asyncio.to_thread(...)` | `grep -c asyncio.to_thread routers/*.py` = **11**（≥9）；AST 守卫：async 端点内无直接阻塞调用 |
| **U-06** | `orchestrator` 检索 Key 对齐 `search.py` 回退口径（非 bocha 复用服务商 Key）+ 无 Key 生成前告警 | 离线管线 16/16 通过（回退口径不破坏注入式测试） |
| **U-07** | 查重未消除题改为**可见标记「⚠ 疑似重复」+ 人工复核留痕**（不剔除） | 守卫断言产物含标记；离线管线题库规模零回归 |
| **U-08** | `setCounts()` 限定 `[data-t]` 按钮 + label 兜底 | 守卫断言旧写法不得回归 |
| **U-09** | 新建 `core/projects.py`（建课能力下沉）；`core/gap.py` 删除两行反向依赖；DELETE SQL 下沉；路由层 8 处 `dbs.migrate()` 删除 | AST 守卫：`from ..routers` 零命中 / 路由无 `cur.execute`、`dbs.tx(`、`dbs.migrate()` |
| **U-10** | `_run_project_impl` **727 → 342 行**（抽 4 个阶段函数：`_stage_websearch` / `_stage_generate` / `_stage_gate1` / `_stage_qc_fix`）；`export_paper_html` **440 → 35 行**（抽 3 个片段函数） | AST 判据：**全仓无 ≥400 行函数** ✓；`export_paper_html` 拆分经**逐字节等价校验**（14 用例 × 2 导出函数输出完全一致）；新增 2 条 AST 守卫 |
| **U-11** | 新增 `docs/adr/ADR-006-retire-json-store.md` + `docs/README.md` 索引 | ADR 含四节；索引已同步 |
| **U-12** | `pytest-cov` + `[tool.coverage.*]` + CI `--cov-fail-under=80`；新增 `tests/test_stream_wiring.py`；handoff §4.15 + §5.1 | 覆盖率 **82.46%**（门槛 80% 通过）；**反向验证**：移出 `dedupe.end` → 必红 |
| **U-13** | `export_anki` 的 `id` 转义；新增 `_esc_md()` 覆盖 MD 三条路径；新增 17 条 XSS 守卫 | 探针实测：题库/押题卷 HTML 无泄漏；Anki/MD 缺口已修；**反向验证**：回退 Anki 转义 → 必红 |

### 4.3 P2 批次

| 编号 | 改动 | 验收实测 |
|---|---|---|
| **U-14** | 迁移 **v7**：v1 五张表补齐索引（`MIGRATIONS` 追加 7，`_V7_UP/_V7_DOWN` 齐备） | `migrate()==7`；`sqlite_master` 可见 5 个新索引；回滚路径已验证 |
| **U-15** | 新建 `core/errors.py`（record/snapshot/swallow/redact）；**31 处静默 `pass` 全部留痕**；新增 `GET /api/diagnostics/errors`；`llm.py` 不再回显模型原文；日志/回显统一脱敏 | 静默 pass **31 → 0**；守卫断言 ≤5；诊断端点 200；脱敏断言通过 |
| **U-16** | `requirements.lock`（38 项）/ `THIRD_PARTY_NOTICES.md` / CI `pip-audit --strict` / `.github/dependabot.yml` | `pip-audit`：**无已知漏洞**；`pip check` 零输出；YAML 校验通过 |
| **U-17** | ⏳ **部分完成**（见 §5）：ESLint 静态防线已落地（`package.json` + `eslint.config.js` + CI `npm run lint --max-warnings 0`）；**上线即抓到一处真实缺陷**（`learn.js` 调用不存在的 `rexAnalyzeRender()`）并清理 5 处死代码。**ES Module 化 + 按域拆分未做**（需浏览器回归网） | `npm run lint` → **0 error / 0 warning**；`node --check` 四个 JS 全部通过；CI YAML 校验通过 |
| **U-18** | 题库页头部改为「共 N 题（M 张卡，其中组内子题 K 道）」 | 守卫断言口径一致 |
| **U-19** | 错因缺省 `reasoning` → `unknown`；押题卷回流带 `unanswered`；新增「未作答/未标注」标签 | 守卫断言缺省入库为 `unknown` |
| **U-20** | ⏳ **未执行**（见 §5） | — |
| **U-21** | 删除 `test_r4_batch3.py` 的 `_run_async`，统一走 `run_coro` fixture | `grep _run_async tests/` 仅剩说明注释 |

### 4.4 收尾批次（U-20 / U-22 / U-24 + AGPL，本轮回填）

| 编号 | 改动 | 验收实测 |
|---|---|---|
| **U-20** | 新建 `routers/data.py`（数据目录/占用统计 + 一键备份 zip + 打开目录 + 清空二次确认 + 自动备份）；`main.py` 挂载；前端「我的」新增数据管理区；`tests/test_data_mgmt.py` | `GET /api/data/summary` 200 且含 size/counts/by_subject/backups；`POST /api/data/clear` 错误确认短语 → 400、正确短语 → 200 且自动备份；`test_data_mgmt.py` **8 通过** |
| **U-22** | `library.py` 新增 `is_kp_dirty/is_mistake_dirty`（`?` 乱码串/可逆 cp1252/`data_broken`）；`get_mastery_view` 分母排除脏知识点、`routers/library.py::subjects()` 聚合排除脏错题/知识点 | 新增 `tests/test_u22_dirty_data.py` **4 通过**；回退验证：乱码科目不再进 `???` 下拉、不虚增掌握率分母；关联 66 项（dashboard/library/subject_delete/api/data_mgmt/review）全绿 |
| **U-24** | `logging_setup.py` 新增 `RedactingFilter`（sk-`***` / `Authorization|api_key: ***`，挂文件/控制台 handler）；`medkit.spec` excludes 显式化 + 体积断言 | 新增 `test_logging_setup.py` **7 通过**；`test_check_package.py` 新增 spec 体积/许可证断言 **4 通过**（共 11 通过）；美中不足：mypy 配置、jieba/pkg_resources 定时炸弹、re-export 退役清单 → 见 §5 跟踪 |
| **AGPL** | 新增 `LICENSE`（AGPL-3.0 全文）+ `docs/adr/ADR-007-license-agpl.md`；`README.md` / `THIRD_PARTY_NOTICES.md` / `medkit.spec` 同步；CI 许可证检查 | `LICENSE` 含 "END OF TERMS AND CONDITIONS"；`test_check_package` 断言 `"LICENSE"` 进 `datas`；ADP 四节齐全 |

### 4.5 总闸（本轮末次实测）

```bash
python -m ruff check .                                    # All checks passed
python -m pytest -q --ignore=tests/browser --cov=medkit --cov-fail-under=80
#   → 506 passed；覆盖率 82.54%（门槛 80% 通过）
python pack/check-package.py                              # [通过] 无样例/种子/测试/字节码
python -m pip check                                      # No broken requirements found
npm run lint                                              # 0 error / 0 warning
```
> 测试总数 489 → **506**（本轮新增 17：U-20 数据管理 8 + U-22 脏数据 4 + U-24 日志脱敏 7 中的
> test_logging_setup 7 + spec 断言新增 4，去重后净增 17）。
> 覆盖率 82.49% → **82.54%**。

---

## 5. 未执行项与后续计划

> 本轮按优先级完成 **19/24** 项（P0 全部、**P1 全部**、P2 除 U-17 外全部）。
> 后续收尾批次把 **U-20 / U-22 / U-24（代码部分）+ AGPL 计入**，P3 仅剩 **U-23**（浏览器依赖，留档）；
> U-17 的 ES Module 化确认浏览器阻塞（见下）。

### U-10 上帝函数拆分（高 · ✅ 已完成）

- **结果**：`core/orchestrator.py::_run_project_impl` **727 → 342 行**（抽 4 个阶段函数）；
  `render/qbank_html.py::export_paper_html` **440 → 35 行**（抽 3 个片段函数）。
  AST 判据达成：**全仓无 ≥400 行函数**。
- **关键设计**：取消路径统一改为「阶段返回 `cancel_out` 标志、调用方单一出口」——原先散落在
  函数中部的 `return 管线结果字典` 是「不可局部回滚」的主因之一。
- **等价性证据**：渲染拆分做了**逐字节等价校验**（14 用例 × 2 导出函数，SHA 一致）；
  管线拆分依赖 `tests/test_pipeline_offline.py`（16 例，含断点续跑/取消/门禁剔除 e2e）与
  新增 2 条 AST 守卫。
- **遗留**：`_run_project_impl` 仍有 100+ 局部变量与 342 行，进一步收敛需引入
  `PipelineContext` dataclass（原计划的「轻量上下文对象」）；本轮以「先达判据、行为零变更」
  为优先，未做该步——它属于收益递减的重构。

### U-17 前端工程化（中 · **部分完成**）

- **已完成（静态防线）**：`package.json`（仅 eslint + globals，**不引入打包器**）+ `eslint.config.js`
  + `package-lock.json` + CI 步骤 `npm run lint --max-warnings 0`。
  关键设计：经典脚本共享全局作用域 → 配置**逐文件自动收集「其它文件顶层声明 + `window.X =`/`global.X =`」**
  作为共享全局符号，`no-undef` 既抓拼错又不误报跨文件调用；对外暴露的声明以 `/* exported … */` 标注。
- **实测收益**：ESLint 上线即抓到 `learn.js` 调用**不存在的 `rexAnalyzeRender()`**
  （仅在「真题草稿 >200 条」分支触发，运行时必抛 TypeError，长期未被发现），另清理 5 处死代码。
- **未完成（ES Module 化 + 按域拆分 `learn.js` 2402 行 / `review-desk.js` 2299 行）**：
  该步需改 `index.html` 脚本加载方式，而**浏览器层回归网在当前环境不可用**
  （`agent-browser` 不支持 Windows、Playwright chromium 未装；`pip install playwright`
  重试亦告败——见 §4.5）。**确认阻塞并留档**：前置条件是在可用环境跑通 `tests/browser` 44 用例，
  否则 ES Module 化无回归安全网。静态防线已就位，为后续该步提供兜底。

### U-20 数据管理区（中 · ✅ 已完成）

- **结果**：新建 `medkit/routers/data.py`（数据目录/占用统计 + 一键备份 zip + 打开数据目录 +
  清空全部二次确认 + 自动备份），`main.py` 挂载 `/api/data/*`；前端「我的」新增「数据管理」区；
  `tests/test_data_mgmt.py` 8 项全绿。
- **安全边界**：`clear` 破坏性 → 强制匹配确认短语「清空全部数据」+ 自动先做完整备份；
  `restore`（导入恢复）**未在本轮实现**：运行中覆盖 SQLite 有 WAL/线程缓存一致性问题，
  需重启令牌机制与产品交互定案后再做（见 §5 U-20 遗留）。

### P3 打磨项（U-22 / U-23 / U-24）

- **U-22 ✅ 完成**：脏数据（`???`/编码损坏）不再进掌握率分母与科目下拉——见 §4.4。
- **U-23 ⏳ 延后（浏览器依赖）**：首启四卡 2×2、跳过引导对比度、向导后引导条、素材可选框折叠、
  审核台搜索框、年份空筛选隐藏、考试计划驱动行为——均需浏览器回归网验证；当前环境
  `agent-browser` 不支持 Windows（Playwright chromium 未装）→ 留档，前置条件同 U-17。
- **U-24 ✅ 代码部分完成 + 跟踪项**：`RedactingFilter` 单测 + `medkit.spec` 体积/许可证断言已落地（§4.4）；
  **跟踪项**（不阻塞，不进本轮）：① re-export 退役清单——待 ES Module 化（U-17 前置）后统一清理旧
  兼容 re-export；② mypy 配置——全仓类型标注未全量接入，纳入工程化二期；③ jieba/pkg_resources
  定时炸弹——`requirements.lock` 已 pin，待上游发布后评估升级，CI 有 `pip-audit` 兜底。

---

## 6. 本轮新增发现（三报告未覆盖）

| 发现 | 证据 | 处置 |
|---|---|---|
| **`core/gap.py` 的 `from ._common import _write_meta_atomic` 是死代码**（`medkit/core/_common.py` 不存在）——「一键刷薄弱」走到该行必然 `ImportError` | 文件不存在 + 导入语句共存 | U-09 顺带修复（改走 `core.projects.write_meta_atomic`） |
| **`PyMuPDF` 为 AGPL-3.0 / Artifex 商业双授权**（强传染性）；`frozendict` 为 LGPL-3.0 | `importlib.metadata` 读包元数据 | 记入 `THIRD_PARTY_NOTICES.md`；**LICENSE 暂缓写入**（声明 MIT 会与分发 AGPL 组件自相矛盾） |
| **Anki 导出泄漏题目 `id`**（`#html:true` 下可注入用户卡组） | 恶意输入探针：属性逃逸载荷原文出现 1 次 | U-13 修复 + 守卫 |
| **Markdown 导出零转义** | 探针：`_md_question` 直出原始字段 | U-13 修复（`_esc_md`）+ 守卫 |
| `downgrade_to()` 仅支持整库归零，**不支持部分版本回滚** | `db.py:319-331` 逻辑 | 已在 U-14 测试中改为直接验证 `_downgrade_from(cur, 7)` |

---

*整合人：MedKit 改进执行会话 · 2026-09-15。本清单为三份报告的唯一执行口径；U-xx 编号供后续轮次引用（下一轮为 V-xx）。*
