# MedKit 审查问题清单 · 路4 · 数据、隐私与密钥

> 审查对象：MedKit 仓库 `C:\Users\38063\Desktop\medkit`
> 任务书声明基线 HEAD=v0.10.3；**实际取证状态**：HEAD=28c1ab1（`v0.10.1-31-g28c1ab1`），工作树含未提交 0.10.4 WIP（`medkit/__init__.py` 版本号=0.10.4；`db.py`/`orchestrator.py`/`routers/*`/`web/js/*` 等 21 个文件 +504/-119 行）。本清单全部结论以**工作树当前磁盘代码**为准（即本次实际可验证的对象），行号为工作树行号。
> 审查方式：只读静态走读 + 真实 `~/.medkit/` 只读取证（未向其写入任何数据）+ 允许的离线验证。
> 验证基线（本轮实跑）：`ruff check .` → All checks passed；`python pack/check-package.py` → 通过；`python -m pytest tests -q --ignore=tests/browser` → **543 passed in 96.27s**（含 conftest R5-01 家目录防污染哨兵，断言未触碰真实 `~/.medkit`）。
> 保护对象：学生个人数据（学习记录/错题/掌握度/上传教材/真题/素材会话）与自购 API Key。

## 一、问题清单（按严重性降序）

| 编号 | 标题 | 路径(file:line) | 证据/复现 | 影响 | 严重性 | 修复建议 | 状态(已验证/理论) |
|---|---|---|---|---|---|---|---|
| M4-01 | 「清空全部数据」不彻底：exports 备份 / sessions / logs 残留 | `medkit/routers/data.py:33`（`_CLEAR_PATHS = ("library","projects")`）、`:171-175`（逐 rel 清理）；前端文案 `medkit/web/index.html:224`（"清空全部数据（危险）"）、`app.js:357-365` | 走读：`_clear_all_data` 仅 rmtree library/ 与 projects/。`~/.medkit/exports/` 下的科目删除备份（`library.py:715` `subject_<名>_<ts>.json`，含完整题干/答案/解析/掌握度）、批量删除备份（`exports/mistakes_batch_*.json`）、一键备份 zip（`exports/backups/*.zip`，且 zip 内含 config.json 密文 Key）全部存活；`~/.medkit/sessions/*.json`（素材会话，见 `core/sessions.py:3`）与 logs/ 也存活。用户输入「清空全部数据」后期望数据面归零，实际完整学习数据明文残留在 exports/。 | 删除权（PIPL 第47条：处理目的实现后应删除）落实不完整；本机残留面与 UI 承诺不符；换机/送修/同步盘场景下残留在 exports 的完整错题与教材切片仍可被读取。 | S2 | 二选一：① `_CLEAR_PATHS` 追加 `("exports", "sessions", "logs")`（presets/prompts 是否保留需单独决策），并在清空前自动导出一个加密/用户可控位置的最终备份；② 保留备份但在 UI 明确标注「exports 备份目录未清空，如需彻底删除请手动打开目录删除」。 | 已验证（代码路径走读确认；本机 exports/ 当前为空，残留面未实机触发） |
| M4-02 | 一键备份不含素材会话（sessions） | `medkit/routers/data.py:31`（`_INCLUDE_PATHS = ("config.json","presets","library","projects")`） | 走读 `_backup_all` 逐 rel 入 zip；`core/sessions.py:3` 明确素材会话存于 `~/.medkit/sessions/{id}.json`，不在 include 列表。恢复备份后「素材库复用」为空，且无任何提示。 | 备份≠全量：用户换机/重装恢复后，已解析教材切片丢失，需重新 OCR（产生付费额度消耗）。 | S2 | `_INCLUDE_PATHS` 追加 `"sessions"`（注意 backups 分支排除 -wal/-shm 的逻辑对 sessions 目录不适用，直接全量纳入即可）。 | 已验证（代码走读） |
| M4-03 | 迁移前 DB 备份只拷 medkit.db 单文件，未随附 -wal | `medkit/core/db.py:290-299`（`_backup_before_migrate`：`paths += sorted(lib.glob("medkit.db"))`，未 glob `medkit.db-wal`/`-shm`）；WAL 已启用（`db.py:244` 区域 `PRAGMA journal_mode=WAL`） | WAL 模式下未 checkpoint 的已提交事务在 -wal 中；仅 copy2 主库文件的备份是**旧 checkpoint 点**。若迁移失败需回滚该 `.bak`，丢失「上次 checkpoint → 迁移前」之间的提交。窗口：migrate 在服务启动早期执行（`db.py:302`），常规使用窗口小；但若用户此前经历 kill -9 且未触发自动 checkpoint，窗口会扩大。 | 回滚点本身不完整——ADR-005「迁移失败可一键回退」的承诺在 WAL 有未合并数据时打折。 | S2 | 备份前对 LIBRARY_DIR 内连接执行 `PRAGMA wal_checkpoint(TRUNCATE)`，或改为 `sqlite3` online backup API（`Connection.backup()`）生成一致性快照；同时 glob `-wal`/`-shm` 一并拷贝。 | 理论（代码推理；未实机制造未 checkpoint 库验证） |
| M4-04 | run.log 绕过 RedactingFilter 与 errors.redact | `medkit/routers/pipeline.py:79`（`_log_project(base, f"❌ 管线失败：{e}")`）、`medkit/routers/review.py:339`（`❌ 仅重渲染失败：{type(e).__name__}: {e}`）、`review.py:162`；`_log_project` 实现 `routers/_common.py:67-72`（直接 open append，无脱敏） | 走读：`RedactingFilter`（`logging_setup.py:22-26`）只挂在 logging 系统 handler 上；`_log_project` 是裸文件 append，异常串不经 `errors.redact`（`core/errors.py:36-44`）即落项目 run.log。当前 openai SDK 异常串不含 Authorization 头（本轮实机 medkit.log 51KB 抽样 grep `sk-` 0 命中），故无既成泄漏；但任何未来把请求细节带进异常的改动都会直接落 run.log 且不脱敏。 | 纵深防线缺口：项目级 run.log（用户可直接打开、且含在 exports 备份 zip 内）不享受全局脱敏。 | S2 | `_log_project` 写盘前过 `errs.redact(msg)`（截断+掩码），或对 run.log 挂同款 RedactingFilter。 | 理论（当前无 Key 进入 run.log 的实证路径） |
| M4-05 | errs.redact 正则仅覆盖 sk- 前缀与「带标签」形式，非 sk- 形态 Key 不被掩码 | `medkit/core/errors.py:36-39`（`_SK_RE = sk-[A-Za-z0-9_-]{6,}`；`_AUTH_RE = authorization|api[_-]?key\b[:=]`） | 走读：智谱 Key 形如 `xxxxxxxx.yyyy`、博查 Key、MinerU `mr-xxx`、自建/第三方兼容端点自定义 Key 均**不匹配** `sk-` 前缀；若异常串中以「裸值」形式出现（无 `authorization:`/`api_key:` 标签前缀），则原样进 errs.db 与 `/api/diagnostics/errors`（`routers/diagnostics.py:17-20` 直接回放 `errs.snapshot`）。当前 openai/httpx 异常默认不回显请求头，触发需依赖上游或自定义端点把 Key 写进响应体/异常消息。 | 诊断接口会把非 sk- 形态的 Key 明文留在 errs.db（环形 1000 条）并经本机 API 回显。 | S2 | 掩码规则增加：对配置中已保存 Key 的实际前缀做动态掩码（`[k[:4], k[-4:]]` 出现于异常串时替换），或至少把 `mr-`/JWT `eyJ` 前缀纳入正则。 | 理论 |
| M4-06 | DPAPI 加密失败时回退明文落盘，仅靠一次性 toast 提示 | `medkit/core/config.py:78-80`（`except Exception as e: ... # 回退明文，不阻塞保存；用户不感知`）；缓解面 `medkit/web/js/review-desk.js:283-285`（`key_encrypted===false` 时 toast 明文提示） | 实机取证：本机 `~/.medkit/config.json` 主 Key 实际为 `dpapi:AQAAAN...` 密文，DPAPI 路径工作正常。仅在 CryptProtectData 失败（域环境策略/Profile 损坏/非 Windows）时回退明文；前端保存当次 toast 有警告，但不持久化（刷新后无任何"当前为明文存储"的常驻标识）。另：macOS/Linux 恒明文（`config.py:65-66`），README.md:29「API Key 加密存储」未标注「仅 Windows」。 | 用户以为已加密实则明文的窗口期（toast 关后）。 | S3 | ① `key_encrypted=false` 在配置页常驻角标（不止 toast）；② README 与设置页注明「DPAPI 加密仅 Windows 生效；非 Windows/加密失败时 Key 明文存于 ~/.medkit/config.json」。 | 已验证（代码+前端走读；实盘确认为密文） |
| M4-07 | 科目删除不清 syllabus_items / realexam_freq 两表，留孤儿行 | `medkit/core/library.py:728-734`（SQL 模式 DELETE 仅覆盖 `mistakes/knowledge/explains/review_cards/tutor_sessions/cards` 六表）；`core/db.py:77,95`（两表均有 subject 列与索引） | 走读：`delete_subject_with_backup` 未触碰 `syllabus_items(subject)` 与 `realexam_freq(subject)`。删除科目后，其大纲条目与真题考频仍按 (subject, chapter, item) 键留在库中；用户重建同名科目时会看到旧数据"复活"。 | 数据卫生/删除不完整；不构成外泄（本地同库）。 | S3 | 同一事务内追加 `DELETE FROM syllabus_items WHERE subject=?` / `DELETE FROM realexam_freq WHERE subject=?`（备份 JSON 已含两表导出的话同步追加；当前备份 dict 未含此两表）。 | 已验证（代码走读） |
| M4-08 | DPAPI 解密失败时 resolve_key 原样回吐密文，故障提示错位 | `medkit/core/config.py:93-97`（`_unprotect` 失败 `return v`）、`:145`（`resolve_key` 不判前缀直接 return） | 换机/还原/DPAPI 上下文丢失后，`resolve_key("dpapi:xxx")` 解密失败 → 返回字符串 `"dpapi:xxx"` 本身 → 被当作 API Key 发给服务商 → 用户看到「API Key 无效/未授权」（`core/llm.py:225-227`）而非「本机加密上下文不可用，请重新粘贴 Key」。 | 可诊断性：用户无法区分 Key 填错与 DPAPI 不可解。 | S3 | `resolve_key` 检测到以 `dpapi:` 前缀但解密失败时抛 `KeyDecryptError`，由 PUT /api/config 或 /api/llm/test 返回中文提示「加密密钥不可用（换机/还原），请重新粘贴 Key」。 | 理论 |
| M4-09 | ~/.medkit 目录无显式 ACL 加固 | `medkit/core/config.py:18-25`（仅 mkdir parents，不设 ACL） | 实机 ACL 取证：`~/.medkit` 与 `config.json` 均为 `pokt\38063 + SYSTEM + BUILTIN\Administrators` 三条 Allow，无 Users 组条目——依赖用户 profile 继承，标准本地多账户下他人标准账户不可读。但应用从不显式收紧 ACL；若用户把 projects_dir 或备份目录改到同步盘/U盘（`base_url`/`projects_dir` 均可用户改），继承 ACL 可能放宽。 | 边界：默认安全，用户改路径后无主动保护。 | S3 | 文档（README 隐私小节）提示「勿将 ~/.medkit 或 projects_dir 放入公共/同步目录」；可选：启动时对 config.json 与 library 目录显式 `icacls` 收紧到当前用户。 | 已验证（实机 ACL 取证） |
| M4-10 | 启动自动检查 GitHub Releases 无关闭开关 | `medkit/core/update.py:51-57`（GET api.github.com，UA=`MedKit/{ver}`，无用户数据）；触发于启动线程 | 取证：实机 medkit.log 含 `GET https://api.github.com/repos/.../releases/latest` 记录。请求仅带版本号 UA，无学习数据/Key；PIPL 视角属最小化。但无任何设置项可关闭；纯离线/内网用户每次启动仍出网。 | 隐私/合规最小化已达标，仅缺"可关闭"选项。 | S3 | 设置页加「启动时检查更新」开关（默认开），关闭后跳过 check()。 | 已验证（代码+实机日志） |

## 二、敏感数据 → 存储位置 → 加密/脱敏 → 外传出口 清单

| 敏感数据 | 存储位置（实机/代码取证） | 加密/脱敏 | 外传出口（是否明示） |
|---|---|---|---|
| 主 LLM API Key（sk-…） | `~/.medkit/config.json` `api_key`（实机=`dpapi:AQAAAN…` DPAPI 密文，`config.py:61-80`） | Windows DPAPI（CryptProtectData，UI_FORBIDDEN，无 entropy，用户级）；失败回退明文+前端 toast（M4-06） | 仅经 OpenAI SDK `Authorization: Bearer` 头发往 `base_url`（`llm.py:75`）；不进 URL/query；`public_view` 掩码 `key[:4]+…+key[-4:]` 后前端回显（`config.py:176-201`）；日志经 RedactingFilter 掩码（`logging_setup.py:22-26`） |
| 多服务商存档 Key（provider_keys） | `config.json` `provider_keys[pid].api_key`（归档时即加密，`routers/config.py:81-86`） | 同上 DPAPI | `/api/keys` 仅回 `key_masked`（`routers/config.py:146`）；删除路径 DELETE `/api/keys/{pid}` 完整（已走读） |
| MinerU Token | `config.json` `mineru.api_key`（加密存） | DPAPI | 仅发往 `api.mineru.net`（`mineru.py:64/120/140` Bearer）；UI 明示「上传至 MinerU 云端识别，请勿上传涉密材料」（`web/index.html:150-151,327`）；任务制可取消（`routers/ocr.py:195-200`） |
| 博查/检索 Key | `config.json` `web_search.api_key`（加密存） | DPAPI | `websearch.py` 各后端 Bearer 头（:154/:181/:259/:314）；web_search 默认 `enabled=False`（`config.py:37`）；检索词随请求发往所选后端，UI 明示（index.html:327） |
| 学习记录/错题/掌握度（题干、答案、解析） | `~/.medkit/library/medkit.db`（SQLite WAL）+ 迁移前 `.pre-db-*.bak` JSON 留档 | 本机文件，无加密（DPAPI 仅覆盖 Key）；目录 ACL 依赖 profile 继承（M4-09） | 不主动外传；仅在「题目判分后同步」与导出时经本地 API 回前端；一键备份 zip 含 library（`routers/data.py:120`） |
| 上传教材/真题（切片全文） | 项目目录 `~/.medkit/projects/{pid}/`（slices.json/产物）；素材会话 `~/.medkit/sessions/*.json` | 同上本机存储 | 出题时随 prompt 发给 LLM 服务商（用户自购 Key 的端点，用户知情使用）；OCR 扫描件经 MinerU 云端（明示+可取消）；`_analyze_slices` 全量切片仅本地回环（`_common.py:110-114` 注释亦声明无网络开销） |
| 产物 HTML（题库/押题卷/复习手册） | 项目目录 `最终产物/*.html` | 全量转义（路1 已验收）；render/ 目录 grep 无任何 http(s) 外链/CDN 引用（本轮实证 0 命中） | 不主动外传；用户自行打开即本地文件 |
| 运行日志 | `~/.medkit/logs/medkit.log`（RotatingFileHandler 2MB×5，`logging_setup.py:31-41`）+ 项目 `run.log` | medkit.log 经 RedactingFilter（实机 51KB 抽样 0 个 sk- 命中）；**run.log 不经脱敏（M4-04）** | 不上传；errs.db 环形 1000 条经 `errors.redact` 脱敏后由 `/api/diagnostics/errors` 本机回显 |
| 系统/版本信息 | 不主动收集 | — | ① 更新检查仅 UA=`MedKit/{ver}`（无 IP 外用户数据）；② 反馈 mailto 仅手动触发，正文仅版本/platform/时间（`web/js/app.js:231`） |

## 三、交接说明（供路6汇总 Agent 收口）

### 本轮验证过的「已知加固点」真实性（只验证，不重复上报为新发现）
- **DPAPI 加密落盘**：✅ 实机取证 `config.json` 主 Key 为 `dpapi:` 密文；旧明文自动升级路径存在（`routers/config.py:103` 每次 PUT 都过 `encrypt_for_save`；`tests/test_smoke.py:123-139` 闭环回归）。
- **Key 不进 URL / 错误响应脱敏**：✅ LLM 走 SDK Bearer 头；`main.py:147-151` 所有 4xx/5xx 过 `errors.redact`；实机日志无 sk- 泄漏。
- **迁移前自动备份 + 幂等补导**：✅ `db.py:290-299` + `import_from_json`（`.pre-db-import-*.bak` 可回滚）；唯一缺口见 M4-03（WAL 未随备）。
- **OCR 云端明示/可取消**：✅ index.html:150-151/327 明示文案 + `ocr.py:195-200` 取消端点 + 启动孤儿 tmp 清理（`ocr.py:77-89,144-145`）。
- **乱码修复/批量删除备份/科目删除备份**：✅ 备份写入面已走读（`library.py:712-722`），残留面见 M4-01。
- **Host 白名单 + Origin 校验**：`main.py:128-143`，端口回退与 `MEDKIT_PORT` 联动一致（`run_medkit.py:96-119`）。
- **路径穿越防护**：`_safe_pid`（`_common.py:32-38`）+ `project_file` 文件名校验（`projects.py:264-272`）。
- **meta.json 损坏 422 容错**：`_common.py:45-59`。
- **测试不污染真实家目录**：conftest R5-01 家目录哈希哨兵，本轮 543 全绿含哨兵。

### 发现汇总
- **S0 阻断：0 条。**
- **S1 高：0 条。**（Key 全生命周期闭环、外传面、日志脱敏主线均已验证成立；无已验证可利用的窃取/外泄/越权路径。）
- **S2 中：5 条** — M4-01（清空不彻底）、M4-02（备份缺 sessions）、M4-03（迁移备份不含 WAL）、M4-04（run.log 不脱敏，理论）、M4-05（redact 覆盖窄，理论）。
- **S3 低：5 条** — M4-06（DPAPI 失败明文+文档未标注仅 Windows）、M4-07（删科目漏两表）、M4-08（解密失败提示错位）、M4-09（无显式 ACL 加固）、M4-10（更新检查无开关）。

### 触发条件备注
- M4-01/M4-02 触发条件：用户使用「清空全部数据」或「一键备份/恢复」功能（本机当前 exports/ 与 sessions/ 为空，未实机复现，均为代码路径走读结论）。
- M4-04/M4-05 为理论风险：需要上游异常把 Key/敏感串带进 run.log 或 errs 记录，当前 openai/httpx SDK 行为不触发。
- M4-03 触发条件：存在未 checkpoint 进主库的 WAL 数据时执行迁移（kill -9 后立即升级版本）。
- M4-08 触发条件：在 A 用户目录生成 dpapi 密文后拷到 B 用户/换机运行。
