# MedKit 全链路审查 · R3 补充轮（2026-08-29 · 与同日 R3 并行的一轮）

> **本报告与同目录 `ux-audit-r3-2026-08-29.md` 的关系**：同日有两个审查会话并行启动（AGENT_HANDOFF 陷阱 #7 再度自证）。那份 R3 为 3 路（学习中心交互 / 建课-审核-产物 / 后端契约并发），本报告为 4 路（A=壳层设置 / B=建课生成管线 / C=审核台与产物渲染 / D=刷题与学习中心），发现高度互补。**重叠项已在 §0.2 去重标注**，两份合读即为完整 R3。
> 范围：首启向导/连接服务商 → 素材解析/建课/成本 → 生成管线（出题/门禁/质检/修复/手册/渲染）→ 逐题审核台 → 产物（题库/押题卷/手册/Anki/记忆卡）→ 刷题与学习中心 → 设置/更新/反馈。
> 基线：A/B/C 路基于 `f1f8458`，D 路基于 `dff8494`；**全部 P0 与关键 P1 已由主代理在当前 HEAD `b5fab50` 逐项复核**（标注〔✓b5fab50〕，行号以此为准；未标注项行号可能漂移，修复前请按符号搜索定位）。

---

## 0. 验证记录

| 项 | 结果 |
|---|---|
| `pytest`（不含 browser）@ b5fab50 | **327 passed**（13.5s；上游 deprecation warning ×2） |
| 关键项在 b5fab50 复核 | B20 / B21 / B22 / C-06 / C-10 / D-04 / D-09 / B25 **grep·sed 实地确认仍存在** |
| 本轮新发现 | **83 项**（P0×4 · P1×18 · P2×61），全部定位 file:line |
| 测试盲区注 | 327 绿仍含 4 个 P0：Fake LLM client 不记 usage（掩盖 B22）、项目详情渲染分支无用例（B20）、押题卷 JS 状态逻辑无浏览器用例（R3S-04 群）、`keep=[]` 语义无用例（C-10） |

### 0.1 与既有 R3 报告的冲突需先对账

- **C-10 vs 既有 R3-14**：本轮回在 b5fab50 实测 `routers/review.py:159` 为 `keep_set = set(body.keep) if body.keep else None`（注释明写「空 = 全保留」）——「全部剔除→保存」**静默保留全部**；既有 R3-14 却描述为后端 400「保留题数为 0」仅 toast。两者描述的代码状态不同（并行会话可能已有未合入/后补的修改）。**修复前先对账当前代码**：无论哪种行为，前端都需在保留 0 题时拦截并解释，后端需区分 `keep=[]` 与未传。
- 既有 R3 报告记录 pytest 340，当前 HEAD b5fab50 实测 327——其行号基于另一工作态，引用时同样需重定位。

### 0.2 重叠项去重表（本报告不再单列，修一处即两份销项）

| 本轮编号 | 既有 R3 编号 | 内容 |
|---|---|---|
| A-新1 | R3-05 | 无 Key「载入示例」向导遮罩不关 |
| D-01 | R3-04 | 真题粘贴草稿「跳过」错位/不重渲染 |
| D-17 | R3-02 | 知识点含撇号击穿行内 onclick 按钮 |
| C-03 | R3-01 | 押题卷 compact JSON 缺 id/sid（本轮并入 R3S-04 缺陷群） |
| B24 | R3-09（互补） | 取消不打断 LLM（R3-09=单次请求层；B24=QC/修复/汇总/渲染阶段无检查点）——合并修 |
| C-12/D-23/D16 | —（本轮合并为一条） | subject 非法字符文件名，四处落点统一修 |

---

## 1. P0 —— 立即修（数据正确性命门 / 功能不可用 / 核心承诺失真）

### R3S-01 · 新建/未跑完项目打开「项目详情」整页崩溃〔✓b5fab50〕
- **位置**：`medkit/web/js/review-desk.js:1213`（`showProject`）
- **问题**：`const inWan = (meta.usage.prompt_tokens || 0) / 10000` 无判空——`meta.usage` 只在管线**成功跑完**才写入；1214 行的三元保护来得太晚，1213 行已先抛 TypeError。
- **用户影响**：新建课题（quota 态）、生成中取消、error 态的项目点进详情：配额/stepper/产物/图片素材区**全部空白**，仅剩无效按钮。
- **来源**：R2 批次二 B18「万 token 统一」把计算移出三元分支——**上轮修复引入的回归**。
- **修法**：`meta.usage ?` 判空后两行一并移入。验收：新建未跑管线项目打开详情正常。

### R3S-02 · 审核台「保存/重掷/仅重渲染」后全部图题丢图〔✓b5fab50：review.py 全文 0 处 image_index〕
- **位置**：`medkit/routers/review.py:42`（`_rerender_project`）调用 `qbank_html.export_html(questions, f"{subject} 题库", pid=base.name)` 不传 `image_index`；初跑管线在 `orchestrator.py:663-674` 是传的。
- **用户影响**：上传教材图/心电图的项目，审核台任一重渲染动作后 qbank.html 与押题卷.html **所有图片静默消失**——题干仍写「如图所示」，`render_media`（`qbank_html.py:72`，`if ref and image_index`）连占位提示都不给。
- **修法**：`_rerender_project` 从 slices.json 重建 `image_index`（orchestrator 构建逻辑提为公共函数）；`render_media` 补「有 image_ref 但索引缺图」占位。验收：图题项目审核保存→重渲染后图仍在。

### R3S-03 · token 记账漏掉出题与质检两大头——「费用透明」承诺失真〔✓b5fab50：orchestrator 0 处 copy_context〕
- **位置**：`medkit/core/usage.py:39`（ContextVar）+ `medkit/core/orchestrator.py:236,454-459`（`ThreadPoolExecutor.submit`）。
- **问题**：ContextVar **不随** `submit` 传播到工作线程（D 路实测：worker 线程 `_ACTIVE.get()` 为 None）→ 切片出题与 QC 质检——token 消耗最大的两块——全部漏记。`usage.py:5-6` docstring「随 ThreadPoolExecutor 提交自动传播」与事实相反。
- **用户影响**：meta.usage / run.log「💰 本次实际消耗」/ 项目详情「本次消耗」可能**低估一半以上**；BYOK 向导承诺「费用透明可见」核心失真。叠加 B27（取消/失败路径完全不记账），失败重试前无从估计成本。
- **修法**：submit 处用 `contextvars.copy_context().run` 包装；docstring 纠偏；Fake client 补 token 字段让测试可见。

### R3S-04 · 押题卷作答状态与错题回流——正确性缺陷群（5 项同源，含既有 R3-01）
押题卷「计时作答/判分/错题回流」是核心承诺，前端状态模型成簇缺陷，应**整体重构**（状态按题目 id 而非数组下标建模）：

| # | 位置 | 问题 | 用户影响 |
|---|---|---|---|
| C-07 | `qbank_html.py:502,588-601,620-630` | 续答/旗标按**题目下标**存 localStorage，无题集指纹 | 审核改题/删题/重抽后重开：旧作答**静默套到别的题上**，基于错位答案判分（跨版本串题） |
| C-02 | `qbank_html.py:596-601` | X 型取消全部勾选后 `if(v) st.answers[i]=v` 旧答案残留 | 答题卡/「已答 N」虚高；界面全空却按已取消作答判分 |
| C-03（≡R3-01） | `qbank_html.py:443-462,652-657` | compact 漏传 `id/sid`，WRONG_POOL 去重键退化为题干前 40 字 | 案例组子题模板句前 40 字相同→判错集互相覆盖，**同步错题数少于真实** |
| C-04 | `qbank_html.py:652-657` + `core/library.py:240-254` | 回流 pool 不带 `case_stem/image_ref/data_table`；后端去重键 `qtext[:40]` | 案例子题入错题本无共享题干（不可读）；图题错题无图；同模板子题被静默丢弃 |
| C-18 | `qbank_html.py:504,633-733` | `WRONG_POOL` 在 resetAll/retryWrong 后不清空 | 「重新作答」全对仍回流第一轮错题；「已同步 N 道」虚高 |

- **整群修法**：compact 补 `id/sid`；答案/旗标按 `q.id` 存（`marked` 改集合，顺带修 C-14 单槽）；state 存题目 id 序列，不匹配则提示「卷面已更新，旧作答已失效」并清档（**一次性迁移，勿静默映射**）；空答案 `delete`；pool 补上下文字段入库；后端去重键改 `question_id`（`source_ref` 可承载）；resetAll/retryWrong 清池。
- **验收**：浏览器用例「X 型取消勾选后判分」「案例组两子题同错同步两道」「审核改题后旧作答失效提示」「重做全对不再回流」。

---

## 2. P1 —— 本轮迭代（18 项，重叠项见 §0.2 已剔除）

### 2.1 首启与设置（A 组）

| # | 位置 | 问题 | 用户影响 | 修法 |
|---|---|---|---|---|
| A-新2 | `review-desk.js:91-106` | Key 管理「使用」切换服务商后**不清 api_key 输入框**：刚填的 A 家 Key 未保存就切 B 再「保存配置」→ A 的 Key 归档到 B 名下并生效 | Key 存档污染、401、需重新取 Key | 「使用」成功回调清空输入框（对齐 `doPickProvider:183-187`） |

### 2.2 建课与生成（B 组）

| # | 位置 | 问题 | 用户影响 | 修法 |
|---|---|---|---|---|
| B23 | `agents/medfix.py:13-14,36-42,59-63` | `PROVENANCE_KEYS` 不含 `image_ref/data_table`，修复 payload 也不带——MedFix 改写图/表题后合并结果**天然丢引用** | 图题被修复轮改写后变普通题（题干残留「如图所示」），与 B12「审核台禁掷图题」自相矛盾——管线内部自己丢 | KEYS 追加两字段；payload 带上让 LLM 可回传 |
| B24（与 R3-09 合并） | `orchestrator.py:544-610,627-693`；`medqc.py:121-156` | 门禁①/QC/修复/汇总/渲染阶段**无 cancel 检查**（停止仅在切片边界/QC 前/手册前生效） | 质检阶段点「停止」后管线跑完全部批次与渲染（持续扣费），等来的却是「全部产物生成完成」 | `qc_batch/fix_questions` 传 cancel 逐批检查；停止确认文案按 stage 说明生效时机 |
| B25 | `core/slice.py:31-60`〔✓b5fab50〕 | `flush()` 在块循环体内且每块重置 `cur_title`——PDF 按页成块，章节跨页后切片标题退化为「P2」「P3」（D 路实测 S003\|P2） | 几百页真实教材：切片上下文丢失章节语义；subtopic 取 title[:12] → 大量「P2」类无意义知识点名污染审核台/题库分组/学习中心；也使「按章去重」（B3）不可行 | flush 移出块循环，续页续接当前切片、章节标题跨页传递 |

### 2.3 审核台与产物（C 组）

| # | 位置 | 问题 | 用户影响 | 修法 |
|---|---|---|---|---|
| C-01 | `qbank_html.py:323-366,873` | 题库「🖨 打印」只输出**当前分页**（非当前页内联 display:none，print 规则未重置），且混入筛选按钮与页码文案 | 以为打印全库实际只有当前 50 题且带网页控件；**无任何路径打印完整题库** | `@media print` 强制 `.qpage{display:block}` + 隐藏 `.filters/.qpager` |
| C-05 | `render/apkg.py:118-159` | genanki Note 未固定 guid（默认按字段值哈希），与文件头「稳定哈希防重复导入」承诺不符〔✓b5fab50：全文无 guid〕 | 审核改题/重生成记忆卡后再导 .apkg：Anki 旧卡不更新而**重复新增**，迭代几次卡库翻倍 | `guid=genanki.guid_for(pid+q.id)`；记忆卡用 card id |
| C-06 | `review-desk.js:1146-1158` | `ART_LABEL` 三元组 `[正则,图标,标题]` 解构 `[hit[0],hit[1]]` **下标错位**〔✓b5fab50〕 | 产物网格「图标位」显示 `/^qbank\.md$/i` 字面正则、「标题位」只有 emoji——视觉损坏，B16/D20 目标未达 | 改 `[hit[1],hit[2]]`；补渲染断言用例 |
| C-08 | `qbank_html.py:791-807` | 限时到点且有未答题：`tick()` 每秒触发 `grade()`，取消确认后 `judged` 仍 false → 再弹 | **确认弹窗死循环**：限时交卷没答完，取消后每秒被弹一次 | 自动判分加防重入标志；取消后仅顶部提示 |
| C-10 | `routers/review.py:159` + `review-desk.js:1816-1836` | 前端把剔除编码进 `keep`（drop 恒空数组），后端空 keep=**全保留**〔✓b5fab50；与既有 R3-14 描述冲突，先对账 §0.1〕 | 「全部剔除→保存」返回 ok、toast「已保存并重渲染」——**剔除意图静默蒸发**（与 B13 防误删恰好相反方向的坑） | 后端区分 `keep=[]` 与未传；前端保留 0 题时禁用保存并说明 |

### 2.4 刷题与学习中心（D 组，行号基于 dff8494）

| # | 位置 | 问题 | 用户影响 | 修法 |
|---|---|---|---|---|
| D-02 | `core/gap.py:41` | `plan()` 内 `lib.recommend(limit=40)` **未传 subject** | 「内科」薄弱卷混入外科薄弱点：出题偏移、token 白烧、「薄弱专项」失真 | kps 按 subject 过滤或确认框展示实际构成 |
| D-03 | `agents/medtutor.py:74-110` + `routers/library.py:483-492` | `score_answer` `except Exception` 吞掉一切 LLM 异常兜底 -1 | 断网/Key 失效/限流全表现为「请围绕考点再作答一次」——**反复重答永远失败**看不到原因 | LLMError 上抛 502 中文；仅解析失败走 retry |
| D-04 | `routers/library.py:453-457` + `core/tutor.py:140-158` | 会话先落库再出第一问：LLM 失败留**无问题空会话**；空文本也照常入库〔✓b5fab50〕 | 空会话渲染成「本轮已达成掌握目标」——**从未学习却宣称已掌握**；堆积污染徽章 | 失败回滚；`current.text` 空且无轮次显示「第一问生成失败请重试」 |
| D-05 | `routers/library.py:403-409` + `learn.js:1119-1151` | C6 后端已级联删卡，但 `expDel/expRegen` 确认文案**未提及**（`cards_removed` 未消费） | 记忆卡随删讲解**静默不可恢复消失**，与「可重新生成」承诺相悖 | 确认文案列出「将同时删除派生的 X 张记忆卡」 |
| D-06 | `routers/library.py:88-94` + `learn.js:928-935` | ①UTF-8 **BOM 未剥**——Excel「CSV UTF-8」首列变 `\ufeff题干`→全部行静默过滤→误导性报错；②`.json` 走严格 `MistakeBody`，README 宣称的官方结构 `options:[{label,text}]` 必 422，detail 数组渲染 `[object Object]` | **最常见的 Excel 导出路径直接失败**；官方结构 JSON 不可用 | 解码 `utf-8-sig`；JSON 走 `import-file` 解析器；错误取首条 msg |
| D-07 | `learn.js:104-153` | C16 持久化引入回归：SYL_STD 恢复后 **pill 高亮仍停 HTML 默认**（初始化无 `sylSetStd` 调用） | 以为在看教师重点，实际按官方大纲口径算——覆盖决策被误导（反向同理） | 恢复时调 `sylSetStd(SYL_STD)` 同步选中态 |
| D-09 | `routers/library.py:616 vs 668` | FSRS 记忆卡评分**不回写掌握度**（SM-2 有 `record_review`，`cards_grade` 没有）〔✓b5fab50〕 | 每天记忆卡复习全「记住」，概览掌握率/薄弱点/推荐**纹丝不动**，反复推同一批薄弱点 | `cards_grade` 按 `card.kp_name` 调 `record_review`（quality 同口径映射） |

---

## 3. P2 —— 可排期打磨（61 项按链路分组）

### 3.1 壳层/设置（A 组 19 项，A-新1 已并入既有 R3-05）
- **A-新3** `review-desk.js:295-315`：「保存检索设置」是唯一无 base_url 预校验的保存入口（A4 遗漏面），成功后不清 api_key 输入框。
- **A-新4** `review-desk.js:174-199`：A5 只保护 base_url——切服务商仍**静默覆盖手填模型名**。
- **A-新5** `review-desk.js:14-16`：获取模型列表后已保存模型不在列表时被静默替换为 list[0]。
- **A-新6** `app.js:181-188`：mailto 兜底 toast 让「点复制」，但反馈弹窗已先关闭、复制按钮不可见。
- **A-新7** `app.js:264-299` + `review-desk.js:1113-1115,2059-2061`：`hash=…; showTab(…)` 双入口致 hashchange 再触发——每次切 tab **双倍请求**。
- **A-新8** `review-desk.js:813`：成本预估接口失败静默显示「≈0.0 万 token · ¥0.00」——比不显示更误导。
- **A-新9** `base.css:361` + `app.js:255`：窄屏版本号隐藏，更新红点随之消失，toast 却说「点击左下角版本号」。
- **A-新10** `start.bat:13`：依赖探测只 import 8 包（requirements 实有 11，缺 genanki/jieba/fsrs）→ 双击启动英文 traceback。
- **A-新11** `run_medkit.py:21-30`：pick_port 探测后即 close，与 uvicorn bind 间 TOCTOU 竞态。
- **A-新12** `run_medkit.py:74`：锁文件提示「可删除该锁文件」误导（msvcrt 锁随进程退出自动释放）。
- **A-新13** `review-desk.js:1112-1117`：「创建课题」成功后表单/文件/解析结果全保留——建第二门课极易带错素材。
- **A-新14（含 D-22）** `app.js:115-120`：ESC 关 confirmModal **不触发 onCancel**——薄弱组卷「已创建未运行」提示在 ESC 路径丢失。
- **A-新15** `review-desk.js:2054-2055` + `learn.js:27-28`：A6 焦点守卫不含 SELECT。
- **A-新16** `index.html:251-285`：文件选择器 accept 缺 `.bmp/.text`，与 JS 白名单不一致（B1 修复只补 JS 侧）。
- **A-新17** `index.html:136`：「开启网络检索」硬编码 checked，文案写「默认关」——loadConfig 前一瞬显示为开。
- **A-新18** `review-desk.js:1981`：向导首页宣传「案例组题」，A3/A4 端到端仍不可达。
- **A-新19** `main.py:74-80`：浏览器打开 Timer(0.6) 不等服务监听——慢盘/杀软首启偶发「无法访问此网站」。
- **A-新20** `routers/config.py:108`：cfg.save 失败（磁盘满/只读）未捕获 → 裸 500。
- **A-新21** `core/llm.py:129-139`：测试连接失败是 openai 英文原串；timeout 30 × retries 2 最长 90s+ 像卡死。
- **A-新22** `routers/config.py:66-70`：自定义端点 base_url 留空可保存，调用时才报错。

### 3.2 建课/生成（B 组 10 项）
- **B26** `orchestrator.py:201-209`：`select_paper_stable` 复用不足时**整卷重抽**而非补足——审核剔除 1 题后整卷洗牌（ME-7 要防的漂移在剔除场景仍发生；现有测试构造使两语义不可区分）。
- **B27** `orchestrator.py:509-653` + `pipeline.py:71-79`：取消/出错路径不写 usage——已烧 token 永久失踪（与 R3S-03 合并修）。
- **B28** `orchestrator.py:536-543`：image_ref 门禁条件含 `image_sids` 非空——未传图项目幻觉 image_ref 直接放行，产出伪图题零提示。
- **B29** `review-desk.js:649-674`：parseGroup 按跳过超大文件后的 okFiles 发请求，runOcrJobs 用**未过滤**全量列表按下标对位——OCR 取错文件、结果回填错行。
- **B30** `core/sessions.py:51`：素材会话按 uuid 文件名排序（随机）——找不到最近的会话（B2 未修致同名堆积放大此问题）。
- **B31** `agents/medqc.py:19-25`：QC payload 不含 image_ref/data_table/case_stem——质检对图题/案例组视而不见，分数与质量脱节。
- **B32** `orchestrator.py:654-656` + `medreview.py:33`：手册教材侧=全部切片各截 1200 字拼接后 `[:6000]`——大教材只有最前 ~5 切片进手册。
- **B33** `review-desk.js:1828-1835`：重渲染失败后「正在重渲染 5 项产物…」永久残留（catch 不还原）。
- **B34** `core/mineru.py:125-168` + `routers/ocr.py:84-107`：①上传 PUT 不查 cancel；②任务记录在内存，重启后 404「任务不存在或已过期」、tmp 孤儿；③上传与轮询共用 timeout=120。
- **B35** `review-desk.js:1057` + `pipeline.py:113-121`：试出不含 web/大纲/图片素材且无标注——按试出拍板的用户对正式产物风格变化无预警（B5 只补了 bloom）。

### 3.3 审核台/产物（C 组 8 项）
- **C-09** `routers/review.py:171-188,270-287`：保存/重掷失败回滚只还原 questions_final.json，不回滚半批产物——磁盘题库与押题卷/Anki 版本不一致，与「已回滚」文案相悖。
- **C-11** `routers/review.py:115`：答案归一化第三口径残留——审核台填「B,D」前端放行、保存 400 阻断（D19 未收口）。
- **C-12（统一修，含 D-23 与 R1-D16）** `orchestrator.py:695-700` + `review.py:88-95` + `projects.py:293-295` + `library.py:677-681`：subject 非法字符致 .apkg/记忆卡导出写盘失败——run.log 一行带过、产物网格无卡无告警、兜底端点裸 500。**建议全仓 subject→安全文件名公共函数，四处落点一次修齐。**
- **C-13** `qbank_html.py:261-277`：案例块只渲染首题 media，子题 image_ref/data_table 不渲染——与 MD/押题卷口径不一致。
- **C-14** `qbank_html.py:581-587`：旗标单槽（随 R3S-04 重构一并修）。
- **C-15** `qbank_html.py:121-144`：①B1 组按选项元组并块（两组选项相同即混排）；②全库按 `(type,id)` 重排丢章节序——逐章复习场景顺序错乱。
- **C-16** `routers/review.py:241-244`：B12 只拦旧题——重掷**返回的新题**可带 case_id 成孤立案例子题，无提示。
- **C-17** `render/review_html.py:87-105`：手册 img 白名单放行相对路径（D11 副作用）——LLM 产出 `![](xxx.png)` 直接破图无占位。

### 3.4 刷题/学习中心（D 组 17 项，D-01/D-17 已并入既有 R3）
- **D-08** `index.html:400` + `learn.js:636-649`：概览「刷新」重建科目下拉不回填——**静默重置为「全部科目」」。
- **D-10** `learn.js:1513-1524`：刷题 1/2/3 快捷键对未翻面卡「翻面并立即评分」——误触给首卡打 0 分（污染排期+掌握度）。
- **D-11** `learn.js:1404-1462`：`studyDueBase` 只在切科目时重置——跨天「今日进度 70/100」凭空显示。
- **D-16** `core/library.py:411-414`：文本导入答案正则只捕获单字母——「答案：BD」截成 B 且余文丢弃。
- **D-18** `core/realexams.py:143-159`：考频同键重复确认实际「覆盖/删重」，注释与 toast 却承诺「累加/合并」——分批确认多年真题考频被覆盖低计。
- **D-19** `core/realexams.py:262-279`：≥2 字条目子串命中即标「真题+年份」——题干含「感染/贫血」就被打「真题·2021」。
- **D-20** `core/tutor.py:230-231`：会话达 24 轮上限后轮次静默丢弃，但路由照常计分、LLM 照常扣费。
- **D-21** `learn.js:1130-1145`：「🧠 生成记忆卡」无 busy 禁用——可连点重复调用。
- **D-25** `core/library.py:551-587,654-659`：近期活动缺「入库错题」事件（有定义无写入方），「提问开始」写入不展示。
- **D-26** `learn.js:1350-1356`：判分 retry 重渲染清空用户刚输入的作答——无法在原答案上修改。
- **D-27** `index.html:421-422`：「新增错题」卡头 hint 与卡内 hint 逐字重复两遍。
- **D-28** `learn.js:1417-1424`：`rv_subject` 下拉 init 后永不刷新——新增科目进不了复习筛选。
- **D-29** `README.md:116-158`：README 仍写「六视图（含复习计划）」「Alt+1..6」，v0.8.1 已并为刷题 Tab。
- **性能专项（4 项）**：D-12 `syllabus.py:567-615` 覆盖判定每条目重载全表；D-13 `explain.py:102-151` `index_slices()` 每请求全量重扫+FTS 重建（「查看提示」/每轮判分都触发）；D-14 `library.py:54-56` 错题无分页全量渲染；D-15 `library.py:259-291` 每次进刷题页逐科目全量重算。

---

## 4. R2 修复核验结论汇总（本报告独有贡献；既有 R3 未含）

### ✅ 真实落地（抽查通过，40+ 项）
P0-1 记忆卡门禁 / P0-2 判分自动回流（含 file:// 分支）/ P0-3 已掌握文案 / P0-5 种子打包 / B10 四层答案键校验 / B12 前后端双拦 / B13 全选反选+作用域 / B17 仅重渲染单产物 / D12 apkg B1 选项 / D13 A3A4 标签 / D14 图表占位 / D15 记忆卡导出入口 / D5 localStorage 指纹 / D7 分页按题数 / D8 筛选项目隔离 / D9 大图降采样 / D20 复核清单文案 / D21 答案明文声明 / ME-2 产物开关语义 / ME-9 Anki 门槛 / A9 主题跟随 / A10 预览版比较 / A12 外链离线拦截 / ME-3 提示词 8 个 / C1 概览口径 / C2 讲解 GFM / C3 判分 -1 / C4 按钮兜底 / C7 删除文案 / C8 记忆卡入口 / C11 导入禁用 / C12 概览自动刷新 / C18 会话清理 / C19 按科目刷新 / C22 近期活动 / T4 FSRS 文案 / B5 试出补 bloom（残留 B35）/ B7 配比提示 / B19 防双击 / ME-6 案例组原子抽样 / ME-7 抽样说明（残留 B26）。

**R2 §7 漏标但实际已修**：A3 busy 保护、A6 焦点守卫、A8 toast z-index、B1 文件校验、B4 切片全量预览、B6 预估防抖、B8 stepper websearch、B9 重试费用确认、D1 计时重置、D3 file:// 静默、D6 缺选项明细、D10/D11/D22 手册白名单、C5 删除确认、C6 后端级联删卡、C13 取消去向、C15 自动选科提示、C17 只看未掌握、C20 展开全文、C21 文件路径逐条跳过、R1 的 P2-33/34/35/36/37/38/39/40/42。

### ⚠️ 部分落地/有残留（11 项）
A1/ME-1（遮罩未关，≡R3-05）· A4（检索设置入口漏校验）· A5（模型仍被覆盖）· A11（toast 指向已关弹窗）· B5（web/图题/大纲不带且无标注）· B14（有标记无缩略图/图注/表内容）· B15（无失败定位+文案残留）· B16（说明有了但产物卡被 C-06 视觉损坏）· D17/D18（UI 声明了但管线无 A3/A4 注入路径——「按需输出」构成新文案失真，设计级长线）· ME-6/7（剔除场景仍整卷重抽）· D19（review.py 仍两口径）。

### ❌ 未修（6 项）
B2 同名素材会话堆积 · B3 合并不查重复章（需先修 B25）· C10 成本预估仍点击后展示 · D2 押题卷无 noscript 兜底 · D16 subject 非法字符（→C-12 统一修）· C9 两套自评档位并存（v0.8.1 三按钮+图例已缓解）。

### 修复质量总评
两轮修复绝大多数真实落地且做法正确，但暴露两类系统性风险：①**修复引入回归**（B18→R3S-01、C16→D-07、D11 白名单→C-17、v0.8.1 IA 重组→D-08/D-11/D-28）；②**「文案先行、机制未跟上」**（usage docstring、WRONG_POOL 注释、apkg「稳定哈希」头注、考频「累加」注释——写了没做）。建议把「注释/文档承诺」纳入断言面。

---

## 5. 优先执行清单（与既有 R3 批次合流后的建议）

### 批次 0（半天 · 数据正确性，含既有 R3 批次 1 重叠项）
1. **R3S-01** usage 判空（一行，新建项目详情必崩）
2. **R3S-02** `_rerender_project` 重建 image_index + 缺图占位
3. **R3S-03+B27** copy_context 包装线程池 + 取消/失败出口也 snapshot；Fake client 补 token 字段
4. **R3S-04** 押题卷状态模型整群重构（与既有 R3-01 合并为一个工作包；localStorage 一次性失效迁移）
5. 既有 R3 批次 1 的 R3-01/02/06/07/08 并行推进（互不冲突）

### 批次 1（1~2 天 · P1）
- 正确性：B23（PROVENANCE_KEYS）、B25（切片跨页续接，先于 B3）、C-10（先对账 §0.1）、C-05（apkg guid）、D-04（空会话回滚）、D-09（cards_grade 回写）
- 链路闭环：A-新2（Key 串档）、B24+R3-09（取消全链路）、C-08（限时死循环）、C-01（打印全库）、C-06（ART_LABEL 下标）、D-02（gap 科目过滤）、D-03（判分错误上抛）、D-05（级联删卡告知）、D-06（BOM/JSON 导入）、D-07（pill 同步）

### 批次 2（排期 · P2 分组）
壳层组（A-新3~22）· 生成组（B26~35）· 产物组（C-09/11/13/15/16/17 + D2 + subject 安全文件名统一修）· 学习中心组（D-08/10/11/16/18~28 + D-29 文档）· 性能专项（D-12/13/14/15）

### 设计级长线
A3/A4 端到端或彻底收回文案 · 断点续跑扩展到 QC 后 · 讲解/提问成本预估前置（C10）· FSRS/SM-2/错题本三态统一模型（D-09 只是止血）· 押题卷「全量卷」选项（ME-7 另一半）。

---

## 6. 流程风险与后续建议

1. **同日双 R3 撞车**：两份并行审查已互踩文件名（本报告改道 supplement）；再次自证 AGENT_HANDOFF 陷阱 #7。建议：审查/修复开工前 `git log`+`ls docs/reviews/` 探测并行会话；报告文件名带时间戳+会话标识。
2. **基线三度漂移**（f1f8458→dff8494→b5fab50，本报告撰写期间又出现「340 tests」工作态）：本报告 P0/P1 已在 b5fab50 复核并标注；其余项修复前按符号搜索重定位。
3. **E 路仍未覆盖**（两份 R3 均未做）：README/CHANGELOG 逐条核验（尤其 v0.8.1 新章节）、PyInstaller 打包完整性（v0.8.1 新增数据/依赖是否进 spec）、安全渗透面（Host/Origin 绕过、路径穿越、SSRF、RAG 输出回显转义）、CI 偏差。历史 P0-5 证明打包层最易出「发布版不可用」，建议下轮优先。
4. **测试盲区清单**（327 绿漏 4 个 P0 的根因）：Fake client 不记 usage；项目详情渲染分支无用例；押题卷判分/续答/回流无浏览器用例；`keep=[]` 语义无用例。批次 0/1 修复时逐项补齐。

---

*审查人：MedKit R3 补充轮（4 路并行代理 A/B/C/D + 主代理交叉复核）· 2026-08-29。所有发现定位 file:line；P0/P1 已在 HEAD `b5fab50` 实地复核。优先级按「数据正确性 > 核心承诺 > 链路闭环 > 打磨」排序。*
