# MedKit 路3·医学内容质量与合规审查 · 问题清单（2026-09-17）

> 审查对象：仓库 `C:\Users\38063\Desktop\medkit`，HEAD = `28c1ab1`（2026-09-17 00:25，V-15 拆分后）。
> 审查视角：**医学生拿着生成产物备考**——内容错了 = 学生学错 = 最高后果。
> 方法：只读静态走读 + `tests/fixtures/llm_cases/` 既有样本 + 构造契约反例离线推理验证。
> 基线实测：`ruff check .` ✅ 全绿；`pytest tests -q --ignore=tests/browser` = **543 passed / 0 failed**。
> 约束遵守：未调用真实 LLM；未跑 run_project/parse/ocr；未向 `~/.medkit/` 写入数据（仅 pip 安装只读依赖 + 进程内 import 探针）。

---

## 一、问题清单（按严重性降序）

| 编号 | 标题 | 路径(file:line) | 证据/复现 | 影响 | 严重性 | 修复建议 | 状态 |
|---|---|---|---|---|---|---|---|
| M3-01 | MedQC 质检批次异常降级为「放行」，事实正确性防线可被一次 LLM 故障击穿 | `medkit/agents/medqc.py:118-123`；聚合 `:185-188` | `_qc_batch_once` 内 `except Exception` → 直接返回 `score=50, decision="PASS_WITH_FIXES"`（不 BLOCKED、不计 fail）。聚合 `has_fail` 取自该批 issues，异常批 issues 仅 1 条 `QC_ERR` warn → 整批题目无 F1 事实校验即进产物。触发条件：LLM 超时/限流/JSON 解析失败（本地工具高频发生）。 | 某批 20 题的医学事实（剂量/数值/诊断）**完全跳过事实校验**直接产出；学生按错误剂量备考。同模型自评叠加异常放行 = 事实防线双重失效。 | **S1** | 质检批次异常应返回 `decision=BLOCKED` + 整批题进人工复核清单（而非 PASS_WITH_FIXES）；至少对「异常批」题目标记 `_qc_skip` 并在产物页提示「本批未经质检」。 | 已验证可利用 |
| M3-02 | MedQC 事实性(F1)严重度由 LLM 自评，无独立事实校验锚点 | `medkit/prompts/medqc.md:19`（F1=fail）；`medkit/agents/medqc.py:185-188` | 聚合 decision =「存在 severity=fail 即 BLOCKED」，而某问题是否判 fail/warn **完全由同一（或同族）LLM 决定**。medgen 与 medqc 默认同 provider（`get_client` 仅 role 分流 model，不隔离模型族）。若生成模型把错误剂量写得「看起来合理」，质检模型可能判 warn 而非 fail → 不触发 MedFix、直接 PASS。评分锚点 medqc.md 仅文字描述「与切片冲突→fail」，无程序化数值比对。 | 系统性医学错误（看似合理的错误参考值/剂量）可被自评模型漏判放行；这是「出题正确性」最核心一道防线，本质上是 LLM 自查自纠。 | **S1** | ① 数值型题（答案含数字+单位）增加**程序化数值比对**门禁：抽取答案数值与切片文本数值做存在性/区间比对，不一致即 fail（不依赖 LLM）；② QC 与 gen 强制不同模型档（或至少记录同模型自判风险）。 | 理论风险（机制确认） |
| M3-03 | n-gram 查重只查「题间」，不查「生成题 ↔ 源教材/自备真题原文」 | `medkit/gates/dedup_check.py:43-69`；调用 `medkit/core/orchestrator.py:756` | `check_dup(questions)` 仅对生成集合两两 Jaccard；入参无 `slice_text`/`exam_text`。`build_reference_block`（medgen.py:78-104）用 prompt 硬约束「严禁照抄真题」，但**无程序化校验**。实测：dedup 不接收任何源文本。 | ① 版权：学生上传版权教材 PDF，MedGen 可把教材段落逐字复写进题干/analysis，门禁不拦；② 真题合规：自备真题原文可被「换选项复述」而 n-gram 题间查重不触发（因只跟自家新题比）。 | **S1** | dedup 增加「生成题 ↔ 切片/真题原文」n-gram 比对通道：相似度>阈值即 warn 进人工复核（与题间查重独立）。 | 已验证（入参无源文本） |
| M3-04 | 答案键字母越界（如 answer="F" 但仅 A~E 5 选项）全程不拦截 | `medkit/core/schema.py:126-143`；`medkit/gates/options_check.py:90-95` | 契约 `_answer_letters("F")` 只保留 A~E → 得空串，非 X 分支仅查 `len(letters)>1`，空串通过；options_check R0 仅查 answer 非空 + 单字母，**不校验字母是否落在选项数内**。实测探针：`QuestionItem(answer="F")` 校验通过、options_check fail codes=空。D2 `_render_precheck`（orchestrator.py:70）仅查 answer 非空。 | 答案键与选项无对应关系：题库页显示「答案：F」无对应选项；押题卷判分时学生只能选 A~E，该题**永远判错**或被判为漏答。 | **S2** | options_check 增 R0b：answer 字母集合必须 ⊆ 有效选项字母集合（A~第N个选项）；X 型同理校验。 | 已验证可利用 |
| M3-05 | 重复选项（两个完全相同选项）不被任何门禁拦截 | `medkit/gates/options_check.py:35-101` | 实测探针：`options=["正常项X","正常项X",...]`（真实长度）→ options_check fail codes=空。R1 只查数量、R4（互斥）仅写在 prompt、代码无重复字符串检测。 | 同一题出现两个相同选项，正确答案可指向任一重复项，命题不成立；学生无从区分。 | **S2** | options_check 增 R1b：strip 后选项去重计数 < 数量 → fail。 | 已验证可利用 |
| M3-06 | trace_check 只验「切片 ID 存在」，不验「题目知识点 ↔ 切片内容」对齐 | `medkit/gates/trace_check.py:14-38` | 实测探针：编造一道高血压数值题、analysis 标注存在的 `[源:切片S001]`（S001∈known_sids）→ fail_count=0。门禁只做 `SID_PATTERN` 提取 + `sid in known_sids` 存在性比对，不读切片文本、不比对知识点。 | 模型可「挂羊头卖狗肉」：造一道与 S001 无关的题，伪造合法溯源 S001 即通过回查——溯源沦为形式，防幻觉的最后一道机器校验失效。 | **S2** | trace_check 增加「题干关键词与切片文本词重叠度」下限（如 Jaccard<阈值 → warn/fail），把「引用了存在但无关的切片」标出来。 | 已验证可利用 |
| M3-07 | 全产品无医学免责声明；README 所称「体检警告」实为切片质量体检而非医学安全提示 | `medkit/web/js/review-desk-review.js:605-694`（首启向导三步）；`medkit/routers/_common.py:94` | 全仓 grep「免责/不构成诊疗建议/就医/医嘱/不替代」于前端 index.html+全部 js = **0 命中**。首启向导三步（625/639/652）只讲功能与费用，无「AI 生成、内容可能有误、仅供学习、不替代教材/指南/医嘱」。README:120「体检警告」指向 `_common.py:94 切片质量体检`（章节结构/内容量/token 估算），与医学安全无关。 | 学生把 AI 生成的「数值速查/临床路径/讲解」当作权威临床依据；误学错误数值无任何风险提示兜底。 | **S2** | ① 首启向导加一页「内容免责声明」（AI 生成、可能有误、以最新教材/指南为准、不替代医嘱）；② 复习手册「数字与标准速查」表页头加免责标注（见 M3-08）。 | 已验证 |
| M3-08 | 复习手册「数字与标准速查」表无「以教材/指南为准」标注 | `medkit/prompts/medreview.md:13`（`## 四、数字与标准速查`） | medreview.md 规则仅写「事实仅来自给定素材，不编造；数值与题库答案一致」，**无数值类内容须复核/标注来源的指令**。对照 medexplain.md:20 有「以最新教材/指南为准」，medreview 缺失。该表正是学生背诵数值的高危产物。 | 学生直接背诵 AI 浓缩的参考值/阈值，若切片本身有误或浓缩失真，无任何提示。 | **S2** | medreview.md 在「数字与标准速查」节要求表注「⚠ 数值依据生成教材，请以最新版教材/指南复核」。 | 已验证 |
| M3-09 | conflict 网络素材仅 prompt 层「勿用答案」，仍整体注入 MedGen，无程序化硬隔离 | `medkit/core/orchestrator.py:606`；`medkit/core/websearch.py:533-543` | `digest_for_prompt` 把全部 materials（含 `conflict=True`）拼成文本，仅以标签 `【与教材冲突-勿用答案】` 区分；orchestrator.py:606 全量进 `web_materials_text` → medgen `build_web_block` 注入 system。无代码把 conflict 条目从注入文本中剔除。MedFix 不接收 web 素材（medfix.py:37-48 仅 source_slice），此点为好。 | LLM 可忽略「勿用答案」标签，以与教材矛盾的网络素材为依据出题/定答案；「绝不自动改写」声明在 MedGen 注入链路上仅靠 prompt 自觉。 | **S2** | digest_for_prompt 默认剔除 conflict 条目（仅在人工复核清单留档）；或注入时把 conflict 条目移到单独「禁用作答」区并强制隔离。 | 已验证 |
| M3-10 | 押题卷无「AI 生成、非官方真题」标注，「押题」措辞存在误导 | `medkit/render/qbank_html.py:565`（标题 `{subject} 押题卷`）；`:573` | 页头有诚实的「答案内嵌源码、请勿用于正式考试」（答案明文声明 ✅ 属实），但**无「本卷由 AI 生成、非官方真题、不保证命中考点」**。标题直接用「押题卷」。 | 「押题」易被学生理解为「预测真题」，实际是按章节+Bloom 抽样的 ≤50 题新题；缺 AI 生成明示有考试伦理误导风险。 | **S3** | 押题卷页头/标题加「AI 生成 · 非官方真题 · 仅供练习」角标。 | 已验证 |
| M3-11 | medgen 无「不确定即不出题」硬规则，仅「不编造、避开该细节」 | `medkit/prompts/medgen.md:41-43`（HC-5） | HC-5「未提供数据一律不编造，改用题干陈述原文出处或避开该细节」——允许「避开细节」，未明确「无法确定即放弃本题」。 | 模型在边缘知识点上可能出「半对」题（避开了硬数值但机制仍错），正确偏好是宁缺毋滥。 | **S3** | HC-5 增补「对考点把握不足时宁可不纳入本切片题量，不得强行出题」。 | 理论风险 |
| M3-12 | syllabus_extract.md 含 `{subject_text}` 文档占位符却走 `load_prompt` 非 `render_prompt`，渲染口径不一致 | `medkit/prompts/syllabus_extract.md:8`；`medkit/core/syllabus.py:198` | 其余 7 个提示凡有占位符均经 `render_prompt` 单次替换；本文件用 `load_prompt`（不替换），`{subject_text}` 仅作文档性 brace 保留在 system prompt。当前不泄漏（输入在 user 消息下发），但与 NX-06「占位符以 prompts 为准」口径不一致，后续若误接 render_prompt 又漏传变量即泄漏。 | 维护性/契约一致性隐患；当前无实际内容泄漏。 | **S3** | 统一为 render_prompt 传 `subject_text`（或把文档 brace 改为反引号纯文字避免歧义）。 | 已验证（无泄漏） |

---

## 二、门禁「输入 → 规则 → 反例验证结果」表

| 门禁 | 规则（file:line） | 反例构造 | 结果 |
|---|---|---|---|
| options_check R1 选项数 | `options_check.py:48-51` 数量≠5 → fail | 3 选项题 | ✅ **可拦截** |
| options_check R7 绝对化/截断 | `:53-57` 绝对词；`:64-73` 半截 | 含「以上都对」 | ✅ **可拦截** |
| options_check R9 数值缺单位 | `:86-89` | 「LVEF<40」 | ⚠️ **仅 warn**（不阻断，进 MedFix 仅因 fail+dup 才修——R9 是 warn，实际不触发 MedFix，仅留痕） |
| options_check R2 长度失衡 | `:81-84` | 长短选项差>1.5x | ⚠️ **仅 warn** |
| **answer 字母越界** | 无对应规则 | answer="F"（5 选项） | ❌ **可绕过**（M3-04） |
| **重复选项** | 无对应规则 | 两个相同选项 | ❌ **可绕过**（M3-05） |
| 答案与题干矛盾/事实错误(F1) | 程序化门禁无；仅 MedQC F1（LLM） | 错误剂量题 | ❌ **未覆盖**（靠 LLM 自评，见 M3-01/M3-02） |
| bloom_check 分布偏差 | `bloom_check.py:24-32` 偏差>15% fail（n<10 降 warn） | 全标「记忆」 | ✅ **可拦截**（小题库降级 warn） |
| 单题 bloom 错标 | 程序化无；MedQC D16（LLM） | 应用题标「记忆」 | ❌ **未覆盖**（靠 LLM） |
| trace_check 切片存在性 | `trace_check.py:30-32` sid 不在 known → fail | 引用 S999（不存在） | ✅ **可拦截** |
| **切片存在但知识点错位** | 无对应规则 | 编造题引用合法 S001 | ❌ **可绕过**（M3-06） |
| dedup_check 题间相似 | `dedup_check.py:60-67` Jaccard≥0.8 → warn | 两道近似题 | ✅ **可标记**（warn→MedFix；用尽打「⚠疑似重复」标，不剔除） |
| **生成题 ↔ 源教材/真题原文** | 无对应规则 | 逐字复制教材段落 | ❌ **未覆盖**（M3-03） |
| D2 渲染前终检 | `orchestrator.py:46-76` 非法题型/bloom/空题干/无选项/>6选项/无答案 → 剔除 | 缺答案题 | ✅ **可拦截**（剔除 + 人工复核清单） |

> 说明：R9/R2 为 warn 级，而 `_stage_gate1` 仅对 **fail + dup(warn)** 触发 MedFix（orchestrator.py:783 `actionable = fails + dup_issues`）——普通 R2/R9 warn **不触发修复也不剔除**，仅进 issues 留痕。这意味着「选项长度失衡」「数值疑似缺单位」这类问题即便门禁报了，也不会被自动修复，直接进产物。

---

## 三、交接说明

### S0/S1 摘要
- **S0**：未确认系统性错误医学内容流出。事实正确性主要依赖 MedQC（LLM-as-judge），存在 S1 级防线弱点但非「系统性流出」。
- **S1（3 项）**：
  - **M3-01**：MedQC 批次异常降级 PASS_WITH_FIXES，一次 LLM 故障即跳过整批事实校验。
  - **M3-02**：F1 事实性严重度 LLM 自评、无程序化数值比对锚点，同模型自查自纠。
  - **M3-03**：n-gram 查重不查源教材/真题，版权与「照抄真题」无程序化拦截。

### 已验证 vs 理论
- 已验证可利用（实测探针）：M3-01、M3-03、M3-04、M3-05、M3-06、M3-07、M3-08、M3-09、M3-10、M3-12。
- 理论风险（机制确认、未触发真实 LLM 复现）：M3-02、M3-11。

### 触发条件速查
| 编号 | 触发条件 |
|---|---|
| M3-01 | 任意一批 MedQC LLM 调用超时/限流/返回非法 JSON |
| M3-02 | 生成模型产出「看似合理但医学错误」的数值/诊断（同模型质检） |
| M3-03 | 用户上传版权教材/真题，MedGen 逐字复写进题干/解析 |
| M3-04 | MedGen 输出 answer 字母超出 A~E（LLM 偶发 mis-emit） |
| M3-05 | MedGen 输出两个相同选项 |
| M3-06 | MedGen 编造题并挂一个合法但无关的切片溯源 |
| M3-07 | 任意新用户首启 / 打开产物（产品级免责声明缺失） |
| M3-09 | 开启网络检索且 Round3 检出 conflict，MedGen 忽略「勿用答案」标签 |

### 已验证为真实加固（不作为新发现）
- D2 渲染前终检剔除超限/缺字段题 + 人工复核清单（`orchestrator.py:46-76`）✅
- conflict 条目写人工复核清单、MedFix 不接收 web 素材（`medfix.py:37-48`）✅
- 真题考频「不展示真题原文」（`realexams.py:183-215` 仅章节+条目+频次）✅
- 押题卷「答案明文声明」（`qbank_html.py:573`）✅ 属实
- 网络检索可信源过滤（`websearch.py:46-53` TRUSTED_SUFFIXES/DOMAINS + 【可信】标注）✅
- 占位符单次正则替换防二次注入（`agents/__init__.py:28-34`）✅
- AGPL LICENSE + THIRD_PARTY_NOTICES.md 存在（34523 / 3850 字节）✅

*审查人：MedKit 路3·医学内容质量与合规审查员 · 2026-09-17。基线 HEAD=28c1ab1；ruff ✅；pytest 543 passed。*
