# W 轮补审产物核验记录（2026-09-17）

> **核验对象**：`docs/reviews/审查报告_2026-09-17_W轮补审.md`（40,806 B，2026-09-17 12:20）
> **核验方式**：只读。计数程序化复算 + 25 处以上 file:line 抽检（对齐真身）+ 负向 grep 复跑 + 质量总闸独立复现。
> **核验结论**：**产物可交付**（计数自洽、证据锚点抽检全部命中、总闸独立复现 543 passed），
> 但交付前建议先做 **3 处勘误**（§E）并补记工作区基线（§F.1）。
> **核验人**：独立核验会话（非 W 轮收口 Agent 本身）· 全程未修改任何源码/配置。

---

## A. 产物完整性 —— 通过

| 项 | 实测 | 结论 |
|---|---|---|
| 收口报告 | `审查报告_2026-09-17_W轮补审.md` 40,806 B | ✓ 与摘要"40KB"一致 |
| R8 原报告 | `审查报告_2026-09-17.md` 32,045 B | ✓ 齐备 |
| 风险矩阵 | `风险矩阵_2026-09-17.md` 9,043 B | ✓ 齐备 |
| W1 补审 | `补审_W盲区1_备份恢复_2026-09-17.md` 12,481 B | ✓ 齐备 |
| W2 补审 | `补审_W盲区2_checkpoint持久化_2026-09-17.md` 14,154 B | ✓ 齐备 |
| W3 补审 | `补审_W盲区3_AGPL合规_2026-09-17.md` 18,712 B | ✓ 齐备 |
| 5 份问题清单 | 供应链 20,796 / 前端 12,865 / 医学 15,388 / 数据 16,747 / 服务端 19,171 B | ✓ 齐备 |

**W 轮原始发现编号实测**：W1-01~W1-10（**10**）· W2-01~W2-08（**8**）· W3-01~W3-07（**7**）= **25 条** ✓
与收口报告"25 条原始发现"逐字一致。

---

## B. 计数核验 —— 通过

### B.1 收口报告自身编号（程序化去重）

| 级别 | 报告声明 | 实测编号 | 实测数 | 结论 |
|---|---|---|---|---|
| S0 | 0 | — | **0** | ✓ |
| S1 | 3 | S1-1/2/3 | **3** | ✓ |
| S2 | 33 | S2-1~S2-32（32）+ `SEC-REDACT`（1） | **33** | ✓ |
| S3 | 24 | S3-1~S3-17（区间压缩表达）+ S3-18~S3-24（7 条明细） | **24** | ✓（见 §F.2） |
| 合计 | 60 | 0+3+33+24 | **60** | ✓ |

### B.2 R8 原报告独立复算

| 级别 | 报告声明（原 R8） | 独立复算实测 | 结论 |
|---|---|---|---|
| S1 | 3 | 3（S1-1/2/3） | ✓ |
| S2 | 26 | 26（S2-1~S2-25 共 25 + `SEC-REDACT` 1） | ✓ |
| S3 | 17 | 17（S3-1~S3-17，逐条带 M 编号） | ✓ |
| 合计 | 46 | **46** | ✓ |

46 + 14 = **60** ✓；P0=3 / P1=21 / P2=36 → 3+21+36 = **60** ✓。
P1"原 17 项"清单逐项可数（S2-5、6、1、4、7、8、9、10、11、12、13、14、15、SEC-REDACT、16、18、22 = **17**）✓。

---

## C. 证据锚点抽检 —— 通过（25+ 处全部命中真身）

| 统一号 | 报告引用 | 实测真身 | 结论 |
|---|---|---|---|
| S1-1 | `medqc.py:118-123`、`:185-188` | 118-123 = `except Exception` → `decision:"PASS_WITH_FIXES"`；186-188 = QC_ERR(severity=warn) → `PASS_WITH_FIXES` | ✓ 精确 |
| S1-3 | `dedup_check.py:43-69`；`orchestrator.py:756` | `check_dup(questions, threshold)` 仅题间比对、**无源文本入参**；756 = `lambda ev, qs=questions: dedup_check.check_dup(qs)` | ✓ 精确 |
| S1-2 | `medqc.md:19` | `| F1 | 事实性：…数值与切片不符…| fail |`（LLM 自评，无程序化锚点） | ✓ 精确 |
| S2-13 | `data.py:33`、`:188-191`、`:211` | 33 = `_CLEAR_PATHS=("library","projects")`；188-191 = 逐 child `except OSError` 后继续；**恒 `return ok=True` 实际在 213 行** | ✓ 成立（行号偏 2） |
| S2-14 | `data.py:31` | `_INCLUDE_PATHS` 不含 `sessions` | ✓ 精确 |
| S2-15 | `db.py:290-299`；WAL `db.py:243` | 290-299 = `_backup_before_migrate` 仅 glob `*.json` + `medkit.db`；243 = `PRAGMA journal_mode=WAL` | ✓ 精确 |
| S2-26 | `db.py:280-287`；全仓 grep | 280-287 = `_backup_one` 仅 `copy2`；`wal_checkpoint\|\.backup(` 在 `medkit/` 下 **0 命中** | ✓ 负向证据复现 |
| S2-27 | `db.py:283-286`、`:297-299`、`:310` | `except OSError: return None` 实际 **285-286**（仍在引用区间）；297-299 忽略返回值；310 = 无条件调 `_backup_before_migrate` | ✓ 成立 |
| S2-28 | `db.py:473`、`:494-498` | 473 = `with tx(write=True) as cur:`；**rename 在事务块内**（496-498） | ✓ 精确 |
| S2-29 | `orchestrator.py:392-400` | 399-400 = `except Exception: return set(), []`（无 .corrupt、无 run.log） | ✓ 精确 |
| S2-30 | `orchestrator.py:1026-1027` | `if meta.get("stage") == "done": return …` 仅此一处特判 | ✓ 精确 |
| S2-31 | `orchestrator.py:973-978` | 973-978 = `with ckpt_lock: done_sids.add(sid_r)` **无条件**（未判 `qs` 为空） | ✓ 精确 |
| S2-32 | `projects.py:244-248`；`pipeline.py:41-46` | delete 侧仅 `RUNNING.get(pid)`（无锁）；启动侧 41 = `with RUN_LOCK` | ✓ 精确 |
| S3-19 | `core/sessions.py:44-45` | 44-45 = `(...).write_text(json.dumps(...))` 直写，未走 `write_json_atomic` | ✓ 精确 |
| S3-24 | `state.py:10-12` | `RUNNING`/`RUN_LOCK`/`CANCELLING` 确在 `medkit/state.py`（非 `core/state.py`） | ✓ 该半条成立 |
| S2-22 | `notices:20-57` vs lock 38 项 | notices 20 行 = `## 运行时依赖闭包（34 项）`，表体 34 条；`requirements.lock` **38 个包条目**（42 行，含 4 行注释）→ 缺口 **4** | ✓ 数字自洽 |
| S2-24 | `requirements.lock:36` | 36 = `starlette==1.2.1` | ✓ 精确（修复线见 §E.3） |
| S2-19 | `ci.yml:63-67`；`check-package.py:45-47` | 63-67 = R6-11 注释 + `python pack/check-package.py`；45-47 = `if not root.exists(): print("[跳过]…"); return 0` | ✓ "CI 空操作"成立 |
| S2-16 | `ci.yml:51` vs `ci.yml:36` | 51 = `pip_audit -r requirements.txt`；36 = 装 `requirements.lock` | ✓ 审计图≠产物图成立 |
| S2-6 | `main.py:139-143` | Origin 校验在 `if request.method not in ("GET","HEAD","OPTIONS")` 内 → GET 不查 | ✓ 精确 |
| AGPL | `README.md:24-25`、`borrow-rules.md:59`、`ADR-007:15-16`、`index.html:53-57`、`LICENSE:540-551`、`notices:16` | 逐行命中（"同源同规格" / "同源项目" / "本项目不提供面向公众的网络化服务" / 外链无许可告知 / AGPL §13 原文 / PyMuPDF 条目） | ✓ 全部精确 |
| 版本口径 | `__init__.py:3`=0.10.4、README:12,99=0.10.3 | 实测 `__version__="0.10.4"`；`pack/version.iss`=0.10.4；README 仍 0.10.3 | ✓ S3-15 成立 |
| 前端 4 条 | `qbank_html.py:101-103`、`:208`、`:750,755-756`、`:876-880` | 分别为 `{ref}` 直插、export_md 的 `image_ref` 直插、`el.innerHTML=…last.score…`、`retryWrong()` 信任 localStorage | ✓ 全部精确 |
| S2-12 | `orchestrator.py:606`；`websearch.py:533-543` | 606 = `digest_for_prompt(materials)`；533-543 = conflict 仅加文本标签「与教材冲突-勿用答案」，素材**整体注入** | ✓ 精确 |

---

## D. 质量总闸独立复现 —— 通过

```
系统 Python 3.12（C:/Users/38063/AppData/Local/Programs/Python/Python312/python.exe）
python -m pytest tests -q --ignore=tests/browser --basetemp=<全新目录>
→ 543 passed, 2 warnings in 273.60s
```

与报告声明"**543 passed / 0 failed**"**逐字一致**（含 2 条 warning：StarletteDeprecationWarning、jieba pkg_resources）。
浏览器层（报告称 35 passed）本轮未复跑，不构成交付阻塞。

---

## E. 需勘误项（3 处，交付前建议先改）

### E.1 附录 A 的 `.bak` 数量与体积不符 —— **实证**

**实测**：`~/.medkit/library/` 下存在 **2 个** 同批（时间戳后缀 `pre-db-20260917-120655` 完全一致）新增文件：

| 文件 | 体积 | 说明 |
|---|---|---|
| `medkit.db.pre-db-20260917-120655.bak` | **1,236,992 B ≈ 1.18 MiB** | 备份时刻的**整库副本** |
| `slice_index.json.pre-db-20260917-120655.bak` | **7,015 B** | 同一 `_backup_before_migrate` 调用顺带备份的活 JSON |

- 报告附录 A 与 W2 报告 §4 均只写"新增文件 `medkit.db.pre-db-…bak`"**1 个** → 应改为 **2 个**（`_backup_before_migrate` 备份 `LIBRARY_DIR/*.json` + `medkit.db`，当时活 JSON 仅 `slice_index.json`，故恰为 2 个）。
- 摘要口径"`.bak` 文件（约 **28KB**）"**有误**：28KB 是 **DB 体积增量**（1,236,992 → 1,265,664 = +28,672 B），`.bak` 本身是 1.18 MiB。现行 `medkit.db` = **1,265,664 B** ✓ 与报告数字完全吻合。

### E.2 "未修改任何用户文件"与报告自身叙述矛盾 —— **内部不一致**

- 报告头声明"全程只读…**未向真实 `~/.medkit/` 写数据**"；附录 A 第 3 点又写"**未删除/修改任何用户文件**"。
- 但附录 A 第 2 点自己已写"真实库文件由约 1,236,992 字节变为约 1,265,664 字节（**系 migrate 的 schema 升级**）"——**该库确已被修改**。
- 建议统一改为：**"未删除任何用户数据；`medkit.db` 经一次 `migrate()` schema 升级（字节数已变），原库有 `.bak` 可回滚"**；报告头的"只读"限定应加脚注指向附录 A。

### E.3 S3-24 的"文档漂移"指控不可复现 —— **疑似假阳性**

- `docs/AGENT_HANDOFF.md` 中全部 **31 个** `tests/*.py` 引用**逐个存在**（含 `tests/browser/test_substeps_panel.py`、`tests/browser/test_start_exams.py`）。
- `test_pipeline_writes_substeps_e2e` / `test_pipeline_progress_substeps` 是**测试函数名**，非文件；实际存在于 `tests/test_pipeline_offline.py:643` / `:618`。
- `审查任务书/` 中 2 个 `tests/*.py` 引用亦**全部存在**。
- → "引用已不存在的测试文件"**无任何证据**。该条另一半（`medkit/state.py` 而非 `core/state.py`）成立。
- **建议**：S3-24 改为单点指控"state.py 实际路径与任务书不符"，或**撤条**并把计数从 24 调为 23（若撤，S3 合计与总计数需同步改为 59）。

### E.4（附加）S2-24 的 CVE 修复线精度

- 报告写"starlette==1.2.1 **低于 CVE-2026-54283 修复线 1.3**"。
- 外部核验（GHSA-82w8-qh3p-5jfq / CVE-2026-54283）：受影响区间 **≥0.4.1 且 <1.3.1**，**修复版本为 1.3.1**。
- 结论方向正确（1.2.1 受影响），但**升级目标必须写 ≥1.3.1**，否则按"1.3"修复会踩空。
- **补充可达性证据（供排期用）**：本仓未直接调用 `request.form()`，但 `routers/parse.py:23-24`、`ocr.py:149-150`、`syllabus.py:139-141/212`、`projects.py:346-347`、`library.py:115/237` 的 `UploadFile=File(...)`/`Form(...)` 会经 FastAPI 走 `request.form()` 解析 → 触发面真实存在（仍受 `127.0.0.1` 回环限制）。

---

## F. 可复现性问题（3 处，建议补记）

### F.1 基线未钉住工作区（**最重要**）

报告以 `HEAD = 28c1ab1` 为取证基线并称"全程只读"，但实测工作区有 **20 个未提交改动**，且均为**真实改动**（非 `core.autocrlf` 换行噪声）：

```
CHANGELOG.md +57/-0        medkit/core/db.py +23/-8        medkit/core/orchestrator.py +90/-29
medkit/routers/library.py +4/-0   medkit/routers/pipeline.py +9/-3   medkit/routers/review.py +14/-3
medkit/web/js/learn-review.js +64/-20   ...   tests/test_pipeline_events.py +74/-4
tests/test_db.py +14/-0    tests/test_mistake_batch.py +13/-0    tests/test_s1_render.py +12/-0
tests/test_u_batch3_render_safety.py +18/-0    pack/version.iss +1/-1   medkit/__init__.py +1/-1
```

- 全部 mtime 落在 **10:42–10:45**，**早于**报告成稿时间（11:27–12:20）→ 报告并非"改动了工作区"，但其所有 file:line 证据实际取自 **「28c1ab1 + 20 文件未提交改动」的混合态**。
- 风险：这些改动一旦被提交或被还原，**行号会漂移、结论会失配**（本项目已有 `data.py:211→213` 这类 2 行漂移的先例）。
- **建议**：基线写成 `28c1ab1` **+ 20 个文件的工作区 sha256 清单**（或先提交这些改动再冻结基线）。

### F.2 S3-1~S3-17 缺逐条编号映射表

整合报告 §3.3 仅以"S3-1~S3-17（M1-05/06/07、M2-03/04/06、M3-10/11/12、M4-06~10、M5-08/11/12）"压缩表达。
我按 R8 报告 §3.3 逐条核对，**17 条 ↔ M 编号完全对应**（M1-05→S3-1 … M5-12→S3-17），映射可恢复；
但**整合报告自身不自洽可查**，建议补一张 S3 编号↔原编号对照表（S2 段已有此表，S3 段缺失）。

### F.3 复测判据表述有误

§九 第 3 步写 `THIRD_PARTY_NOTICES 行数==requirements.lock 行数(38)`：
- `THIRD_PARTY_NOTICES.md` 实为 **68 行 / 34 个依赖条目**；`requirements.lock` 实为 **42 行 / 38 个包条目**。
- 正确判据应是"**notices 依赖表条目数 == lock 包条目数**"，且当前 **34 ≠ 38 正是 S2-22 的缺口本身**——作为"修复后判据"需先写明目标值（38）。

---

## G. 交付判断

| 维度 | 结论 |
|---|---|
| 产物完整性 | ✅ 通过（收口报告 + R8 + 风险矩阵 + 3 份补审 + 5 份清单齐备） |
| 计数自洽性 | ✅ 通过（60 = 46+14；P0/P1/P2 = 3/21/36；R8 46 独立复算一致） |
| 证据锚点 | ✅ 通过（25+ 处抽检全部命中真身；2 处行号偏 2 行仍在引用区间内） |
| 总闸复现 | ✅ 通过（543 passed，与声明逐字一致） |
| 披露准确性 | ⚠️ **3 处需勘误**（.bak 数量/体积、".bak 未修改"表述、S3-24 撤条） |
| 可复现性 | ⚠️ 建议补记工作区基线哈希（F.1）、S3 映射表（F.2）、修判据（F.3） |

**结论**：报告主体结论（S0=0 / S1=3 医学事实门禁 / W 轮新增备份与 checkpoint 两条链 / 盲区三移交产品法务）
**证据充分、可交付**；建议先落 **§E 的 4 处勘误**（其中 E.3 若撤条需同步改计数 60→59），
并在报告头补一行工作区基线哈希，再作为正式交付物分发。

---

## H. 勘误落实状态（2026-09-17 已执行）

> 全部勘误已落入 `审查报告_2026-09-17_W轮补审.md`（40,806 B → **50,265 B**），并同步修正其上游文档。
> **计数未变**：修订后复算仍为 S0=0 / S1=3 / S2=32+SEC-REDACT=33 / S3=24，**合计 60**（E.3 选择"改锚点保留"而非"撤条"）。

| 勘误项 | 处置 | 落点 |
|---|---|---|
| E.1 `.bak` 数量/体积 | 1 个 → **2 个**；体积 **≈1.18 MiB**（非 28KB）；补"28KB 是库体积增量"辨析 | 附录 A 表格 + 附录 C.5 |
| E.2 "未修改任何用户文件" | 改为"未删除数据；`medkit.db` 经一次 `migrate()` 升级（字节数已变）" | 报告头执行约束 + 附录 A 第 3 点 + 文末署名段 |
| E.3 S3-24 假阳性 | **撤半条**（测试文件引用不存在的指控）；**改锚点**为 `MedKit_专属审查提示词.md:86` 的 `core/state.py` 路径错误；保留 S3-24 编号 | §3.3 明细表 + P2 修复动作 + 附录 C.3；上游 `补审_W盲区2…md` 的 W2-08 行同步标注"已勘误" |
| E.4 CVE 修复线 | 1.3 → **1.3.1**；补 6 个 `File()/Form()` 端点的可达性证据 | §3.2 明细表 + §九 复测清单 + 附录 C.4；同步修正 R8 报告 2 处、`问题清单_供应链构建发布` 3 处 |
| F.1 基线未钉工作区 | 报告头补"另含 20 个未提交改动"；新增**附录 B** 完整 sha256 清单 | 报告头 + 附录 B |
| F.2 S3 缺映射表 | 补 **17 行**逐条对照表（S3-1~S3-17 ↔ M 编号 ↔ 标题） | §3.3 |
| F.3 复测判据表述 | 改为"notices **条目数** == lock **包条目数**"，写明现状 34≠38 与目标值 38 | §九 第 3 步 |

**同步修改的上游文档**（均为只增不改结论的勘误）：
- `docs/reviews/补审_W盲区2_checkpoint持久化_2026-09-17.md` §3.1 披露段 + W2-08 行
- `docs/reviews/审查报告_2026-09-17.md`（R8）第 149 行、S2-24 行
- `docs/reviews/问题清单_供应链构建发布_2026-09-17.md` M5-01 行、starlette 行、参考链接行

**遗留**：`~/.medkit/library/` 下 2 个 `pre-db-20260917-120655.bak` 仍在，可自行删除（保留亦无害，属回滚点）；
`medkit.db` 已停在 1,265,664 B（schema 已升级，数据未删）。

---

*核验人：独立核验会话 · 2026-09-17。全程只读，未修改任何源码/配置/数据；未调用真实 LLM/OCR/联网检索 API（CVE 信息来自公开 advisory 查询）。*
