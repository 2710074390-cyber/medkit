# MedKit 安全审查 · 路5「供应链、构建与发布」问题清单

- 审查日期：2026-09-17
- 审查员：路5（供应链 / 构建 / 发布）只读静态审查
- 审查基线：`HEAD = 28c1ab1`（`git describe` = `v0.10.1-31-g28c1ab1`；版本单源 `medkit/__init__.py:3` = `0.10.4`）
- 审查方式：只读静态走读 + 允许的离线验证（`ruff`、`npm run lint`、`pack/check-package.py` 只读断言、pytest 离线子集、对 `dist/MedKit` 旧产物做静态检查）。未执行完整打包、未触网构建、未调真实 API、未写真实 `~/.medkit/`。
- 结论一律引用 HEAD 的 `file:line`；旧产物（`dist-old-*` / `build-old-*`）仅作打包机制取证，不作结论依据。

> 版本口径说明（与任务书「HEAD=v0.10.3」的出入）：实际 HEAD 已前进到 `__version__=0.10.4`（今日刚切版，见 `CHANGELOG.md` `## [0.10.4]`），但 `README.md` 仍残留 0.10.3 字样（见 M5-08）。本报告按真实 HEAD `28c1ab1` 取证。

---

## 0. 已验证为「加固点真实有效 / 无新发现」的项（不重复上报）

| 项 | 取证 | 结论 |
|---|---|---|
| 纯净包检查自动运行 | `pack/build.bat:35` 调 `check-package.py`；对当前 `dist/MedKit` 实跑 `[通过]` | 真实有效（但 CI 侧为空操作，见 M5-05） |
| 版本单源 `__init__.py→build.bat→version.iss` | `medkit/__init__.py:3`=0.10.4；`pack/version.iss`=`#define MyAppVersion "0.10.4"`；`build.bat:49` 正则抽取 | 链路本身一致（README 硬编码漂移见 M5-08） |
| AGPL-3.0 + LICENSE 随包分发 | `LICENSE:619` 含 `END OF TERMS AND CONDITIONS`（全文完整）；`medkit.spec:30` `("LICENSE", ".")`；`dist/MedKit/_internal/LICENSE` 存在 | 真实有效 |
| 更新仅提醒+跳转下载页，不自动下载 | `medkit/core/update.py:81-102` 仅返回 `html_url`，前端 `app.js:270` `openExternal(url)` | 无自动替换二进制风险 |
| 邮件反馈不超采 | `app.js:230-232` mailto 正文仅含 版本 / `navigator.platform` / 时间，无 Key、无路径、无用户内容 | 合规 |
| 更新通知 notes 不构成 XSS | `app.js:254` 对 `r.notes` 用 `esc()` 转义后插入 | 安全 |
| pre-release 版本比较 | `update.py:27-78` `_PRE_RE`/`_pre_rank`，正式版>预览版，A10 截断 bug 已修 | 逻辑正确 |
| CI 不暴露 secrets | `ci.yml` 无任何 `secrets.*` 引用；触发为 `pull_request`（非 `pull_request_target`），fork PR 仅只读 token | 无泄露面 |
| 无 artifact 上传 | `ci.yml` 无 `upload-artifact` 步骤 | 不存在「artifact 夹带敏感文件」路径 |

---

## 1. 问题清单（按严重性降序）

| 编号 | 标题 | 路径 (file:line) | 证据 / 复现 | 影响 | 严重性 | 修复建议 | 状态 |
|---|---|---|---|---|---|---|---|
| M5-01 | `requirements.lock` 锁定的 `starlette==1.2.1` 落后于安全修复线（CVE-2026-54283） | `requirements.lock:36` | lock 钉 `starlette==1.2.1`；advisory（GHSA-82w8-qh3p-5jfq）：受影响区间 **≥0.4.1 且 <1.3.1**，「`request.form()` 的 `max_fields`/`max_part_size` 对 `application/x-www-form-urlencoded` 静默不生效，未授权方可发任意大字段数 urlencoded body 致资源耗尽，**fixed in 1.3.1**」→ 1.2.1 受影响。**可达性**：本仓未直接调 `request.form()`，但 `routers/parse.py:23-24`、`ocr.py:149-150`、`syllabus.py:139-141,212`、`projects.py:346-347`、`library.py:115,237` 的 `File()/Form()` 会经 FastAPI 走 `request.form()`。缓解：应用仅绑回环且 `main.py:137-142` Host/Origin 中间件精确拒绝非回环主机与跨源 Origin，跨站 DoS 被拦；本地同机进程已具同等权限 | 依赖维护欠账；理论 DoS（资源耗尽），跨站利用被 Host/Origin 守卫阻断，无 RCE/数据外泄 | S2 | 跟随 FastAPI 允许的上限升级 starlette 至 **≥1.3.1**（注意 fastapi 0.136.x 可能对 starlette 有上限，需联动 fastapi 升版）；升版后重跑 pytest + 浏览器层 | 已验证（版本落后属实；可利用性理论级，已被回环守卫缓解） |
| M5-02 | `pip-audit` 审计的依赖图 ≠ 实际打包进产物的依赖图 | `.github/workflows/ci.yml:51`；`ci.yml:36` | CI 用 `pip_audit -r requirements.txt --strict`，而 `requirements.txt` 全是 `>=` 浮动约束（`requirements.txt:1-12`），pip-audit 解析到的是 **fastapi 拉到的最新 starlette**；但 Windows 实际打包安装的是 `requirements.lock`（`ci.yml:36` `pip install -r requirements.lock`）。两者版本集合不同——M5-01 的 1.2.1 漏洞在「最新解析图」里根本不存在，故审计绿 ≠ 产物无已知 CVE | CVE 闸门对真实 shipped 闭包失明；锁定版落后于 `requirements.txt` 解析结果时，审计给出假绿 | S2 | 审计对象改为实际闭包：`pip_audit -r requirements.lock --strict`（Windows 矩阵），或对 lock 与 requirements 双跑；至少把 starlette 这类传递依赖钉进 lock 并纳入审计 | 已验证（代码事实） |
| M5-03 | `requirements.lock` 无哈希钉版（`--require-hashes`） | `requirements.lock:5-42` | 全部为 `pkg==ver`，无 `--hash=sha256:...`。pip 在同版本号下仍可能拉到不同构建/重打包的 wheel/sdist；lock 只能钉版本不能钉内容，供应链防篡改强度不足 | 篡改同一版本号的发布物时无法被 lock 察觉 | S2 | 改用 `pip install --require-hashes` 或迁 `pip-tools`/`uv` 生成带哈希的 lock；CI 安装时加 `--require-hashes` | 理论 |
| M5-04 | 产物实际依赖闭包 ≠ lock/notices，构建机全局环境污染泄漏进二进制 | `medkit.spec:48-56`（excludes 列表）；`requirements.lock` 全文；`THIRD_PARTY_NOTICES.md:20-57` | `dist/MedKit/_internal` 实含 `email_validator-2.3.0`、`itsdangerous-2.2.0`、`attrs-26.1.0`、`chardet`、`charset_normalizer`、`fontTools`、`tzdata`、`win32`/`pywin32_system32`；但 `grep` 全仓 `medkit/` 对 `email_validator/itsdangerous/chardet/fontTools/attrs/tzdata/win32` **导入 0 处**，且这些包既不在 `requirements.txt` 也不在 `requirements.lock`。即它们来自打包机全局 site-packages 被 PyInstaller 顺带收集，spec 的 excludes 名单未排除它们 → 干净 venv 复现的产物与本二进制不同；二进制内容不可由 lock 复现 | 可复现性破口；构建机若被植入包会随产物分发；notices 无法覆盖真实闭包 | S2 | 在 `medkit.spec` excludes 显式排除上述非运行时包；改用干净 venv/CI 构建以消除环境污染；把「spec 依赖闭包」与 lock 对账入闸 | 已验证（磁盘取证 + 源码 grep） |
| M5-05 | CI 的「打包纯净检查」步骤是自承空操作，真实纯净闸门只在维护者本机 | `.github/workflows/ci.yml:63-67`；`pack/check-package.py:45-47` | verify job 不构建 dist，`check-package.py` 命中 `if not root.exists(): print("[跳过]"); return 0` 恒过（ci.yml 注释 `:63-65` 自承「本 job 不构建 dist…此步即成为真实防线」——但该条件从未成立）。唯一真实纯净断言仅存在于维护者本机 `build.bat:35` | 一旦构建/发布脱离维护者本机（如未来 CI 出包），无任何自动纯净兜底；当前对「谁来保证 shipped 产物纯净」零 CI 约束 | S2 | 增加一个真正执行 `pyinstaller medkit.spec` 的 CI 打包 job（Windows），再让 `check-package.py` 对其产物断言；或至少在 release 流程里固化此步 | 已验证（代码事实 + 注释自承） |
| M5-06 | GitHub Actions 未 pin 完整 SHA，dependabot 不覆盖 actions | `.github/workflows/ci.yml:17,18,76,77,86`；`.github/dependabot.yml:5-40` | `actions/checkout@v4`、`actions/setup-python@v5`、`actions/cache@v4` 均为可变大标签；若 action 被投毒即可在 CI 上下文执行任意代码。`dependabot.yml` 仅配 `package-ecosystem: "pip"`，无 `github-actions`、无 `npm` 条目 | CI 供应链加固不足；actions 自动更新通道缺失 | S2 | 把 `@v4/v5` 钉为 commit SHA（`actions/checkout@<sha>`）；dependabot 增 `package-ecosystem: github-actions` 与 `npm` | 理论（行业惯例级加固缺口） |
| M5-07 | 无可复现 CI 构建、产物未签名，发布完全靠人工 | `medkit.spec:62-74`；`medkit.iss` 全文；`.github/workflows/`（仅 ci.yml，无 release job） | 仓库无 release/publish workflow；exe 由维护者本机 `build.bat` 手建、手工上传 GitHub Releases；`EXE(...)` 无 `codesign`/签名证书，`medkit.iss` 无 `SignTool`；release 不随附 sha256/校验清单。用户除「GitHub over HTTPS」外无 Authenticode/哈希校验手段（Windows SmartScreen 对未知发布者仅提示） | 二进制来源不可在 CI 复现、完整性不可被用户校验；仓库/账号一旦被冒名发布，无签名/哈希可证伪 | S2 | ① CI 起 Windows 打包 job 产出产物并上传 Release artifact；② 对 exe/iss 做 Authenticode 签名（或在 Release 附 `*.sha256` 清单并在 `update.py` 下载链路校验）；③ `update.py` 可在跳转页提示用户核对哈希 | 已验证（无 release workflow、无签名配置） |
| M5-08 | README 版本硬编码漂移（单源链之外的人工文案未同步） | `README.md:12`；`README.md:99`；对照 `medkit/__init__.py:3` | `__init__.py`=0.10.4、`pack/version.iss`=0.10.4、`CHANGELOG.md` 已有 `## [0.10.4]`，但 `README.md:12` 仍写 `MedKit-Setup-0.10.3.exe`、`README.md:99` 仍写「已实现功能（v0.10.3）」。`README.md:81` 自称「版本号单源」，却未把 README 纳入单源 | 文档与实际版本不符；新用户照 README 下载 0.10.3 安装包会得到旧版 | S3 | 把 README 中的安装包名/版本号改为不带版本的描述，或由 build 流程注入；加一条「README 版本 == `__version__`」守卫用例（类比 V-13 单源测试） | 已验证 |
| M5-09 | `THIRD_PARTY_NOTICES.md` 闭包不完整：漏列 lock 4 项 + 实际产物泄漏包 | `THIRD_PARTY_NOTICES.md:20-57`；对照 `requirements.lock:5-42` | 文件自称「运行时依赖闭包（34 项）」；`requirements.lock` 实为 **38** 项，表中漏 `httptools==0.8.0`、`python-dotenv==1.2.2`、`watchfiles==1.2.0`、`websockets==16.0`；且 `dist/_internal` 实际还 ship 了 M5-04 那批泄漏包（含 **`chardet` = LGPL-2.1**，copyleft 却无任何 notice 行）。另 `THIRD_PARTY_NOTICES.md:64` 声称「onedir 默认保留 `dist-info/LICENSE*`」，但 `dist/_internal` 实际仅 9 个 `*.dist-info`，多数包 LICENSE 元数据并未保留 | 再分发时第三方许可证清单不完整；LGPL 组件未留 notice/源码提供说明 | S2 | ① 把 lock 全量 38 项补进表；② 先按 M5-04 把泄漏包从 spec excludes 剔除（同时解决 chardet-LGPL 泄漏）；③ 验证 onedir 是否真的保留各包 `dist-info/LICENSE*`，否则在 spec datas 显式补充或人工随包提供 | 已验证（34 vs 38 磁盘/文本对账） |
| M5-10 | dependabot 不会维护手写的 `requirements.lock` | `.github/dependabot.yml:6-13`；`requirements.lock:1-4` | dependabot pip 生态更新的是 `requirements.txt`（浮动意图）；手写 `requirements.lock` 不是其识别的 lock 格式（非 pip-compile/uv.lock/Pipfile），依赖升级 PR 合并后 lock 不会自动重生，直到人工再生成 → lock 与 requirements 随时间漂移（正是 M5-02/M5-09 的温床） | 锁文件随依赖更新逐渐过时，审计/复现口径失准 | S2 | 改用 `pip-compile`/`uv` 生成可被 dependabot 感知并维护的 lock，或在 dependabot PR 模板里强制「同步重生 requirements.lock + notices」为合并前置条件 | 理论 |
| M5-11 | `npm install` 回退弱化 lock；`--no-audit` 关闭 npm 侧审计 | `.github/workflows/ci.yml:46` | `npm ci --no-audit --no-fund || npm install --no-audit --no-fund`：`package-lock.json` 失配时静默退回 `npm install`（不按 lock 装），且显式 `--no-audit`。运行时前端零 npm 依赖（`package.json` 仅 eslint/globals devDep），影响面仅限开发期 lint 工具链 | 开发工具链可复现性弱；实际不进产物 | S3 | 去掉 `|| npm install` 回退（让 `npm ci` 失败即红，强制 lock 同步）；npm 侧漏洞面小，可接受 `--no-audit` 但宜在注释说明 | 理论 |
| M5-12 | `check-package.py` 黑名单较窄，无敏感文件类与 dist-info/LICENSE 在位断言 | `pack/check-package.py:12-19` | 黑名单仅 `syllabus_seed_306.json/samples/tests/__pycache__/medkit/data/.pyc`，无子串覆盖 `.coverage/.env/*.log/Key 掩码/`docs/`；也不验证「每个第三方包 LICENSE 在位」（该义务靠 `THIRD_PARTY_NOTICES.md:64` 口头「出包后抽查」）。当前因 spec datas 白名单（仅 web/prompts/LICENSE/jieba）而未触发，属安全网边界不足 | 若未来 datas 新增目录引入敏感文件或样例，现有断言未必拦得住 | S3 | 黑名单补 `.coverage/.env/*.log/key_mask` 等敏感模式；增加「产物内 LICENSE 存在」与「禁含 `docs/`/`tests/` 目录」的结构性断言 | 理论 |
| M5-13 | 在线站「同源同规格」声明与 ADR-007「不提供网络服务」表述存在张力（需产品侧澄清） | `README.md:24-25`；`docs/adr/ADR-007-license-agpl.md:16` | README 称公网 `med-review-site.pages.dev` 与本软件产物「同源同规格」；ADR-007:16 据此论证「不提供面向公众的网络化服务，AGPL §13 触发风险低」。若该站面向浏览器分发的 JS/模板派生自 AGPL 的 `medkit/web` 渲染代码，则 AGPL §13 对远端交互用户的源码提供义务可能被触发；本仓库无法核验该站实际代码与许可 | 许可口径在「桌面应用 vs 公网站点」边界上表述不闭合 | S2（理论） | 产品侧明确：站点与本软件「同源」仅指产物数据格式（题库/手册 JSON/MD），而非分发 AGPL 代码；若确实复用了 AGPL 渲染代码，需在站点侧履行 §13 源码提供并补声明 | 理论（跨仓，本仓无法独立证实） |

**无 S0 阻断、无 S1 高优先级发现。** 最高为 S2：全部为「加固不足 / 审计口径偏差 / 可复现性与许可清单完整性」类，无「Key/素材/产物被窃取、恶意依赖稳定进入产物、付费额度被稳定耗尽」的可利用路径（0.10.4 的 RV1 Key 外泄链已在 `CHANGELOG.md` 本日条目修复，非本路职责，仅登记不重复上报）。

---

## 2. SBOM 快照（运行时依赖清单 + 版本 + 风险标注）

> 来源：`requirements.lock:5-42`（声明闭包 38 项）+ 对 `dist/MedKit/_internal` 的磁盘静态取证。版本以 lock 为准；「风险」列仅记录与本路相关的供应链/许可观察，不替代 pip-audit。

### 2.1 requirements.lock 声明的运行时闭包（38 项）

| 组件 | lock 版本 | 许可证（notices） | 风险标注 |
|---|---|---|---|
| fastapi | 0.136.3 | MIT | — |
| starlette | 1.2.1 | BSD | ⚠️ **M5-01**：低于 CVE-2026-54283 修复线 **1.3.1**（回环+Origin 守卫缓解） |
| uvicorn | 0.49.0 | BSD | — |
| uvicorn[standard] extras: httptools | 0.8.0 | MIT | ⚠️ M5-09：notices 漏列 |
| uvicorn[standard] extras: watchfiles | 1.2.0 | MIT | ⚠️ M5-09：notices 漏列 |
| uvicorn[standard] extras: websockets | 16.0 | BSD | ⚠️ M5-09：notices 漏列 |
| openai | 2.41.0 | Apache-2.0 | — |
| httpx | 0.28.1 | BSD-3 | — |
| httpcore | 1.0.9 | BSD | — |
| h11 | 0.16.0 | MIT | — |
| anyio | 4.13.0 | MIT | — |
| sniffio | 1.3.1 | MIT/Apache-2.0 | — |
| pydantic | 2.13.4 | MIT | — |
| pydantic_core | 2.46.4 | MIT | — |
| typing-inspection | 0.4.2 | — | — |
| annotated-doc | 0.0.4 | — | — |
| annotated-types | 0.7.0 | MIT | — |
| PyMuPDF | 1.27.2.3 | **AGPL-3.0 / Artifex 双授权** | 已决策接受 AGPL（ADR-007），合规 |
| python-docx | 1.2.0 | MIT | — |
| lxml | 5.4.0 | BSD-3 | — |
| python-multipart | 0.0.32 | Apache-2.0 | 近期 CVE（53537/53540/42561 等）修复线均 ≤0.0.31，0.0.32 已覆盖；建议随 pip-audit 持续盯 |
| Markdown | 3.10.2 | BSD | — |
| genanki | 0.13.1 | MIT | 唯一被 `==` 钉死的直接依赖 |
| jieba | 0.42.1 | MIT | 版本较老但纯 Python、无已知远程 CVE；setuptools<81 钉版（requirements-dev.txt:6）属兼容钉版 |
| fsrs (py-fsrs) | 6.3.2 | MIT | — |
| certifi | 2026.4.22 | MPL-2.0 | `cacert.pem` 随包，正常 CA 束 |
| tqdm | 4.68.1 | MPL-2.0 AND MIT | — |
| frozendict | 2.4.7 | **LGPL-3.0** | notices 已标注，合规口径已记录 |
| click | 8.4.1 | — | — |
| h11/jiter/idna/distro/chevron/colorama/cached-property/PyYAML/typing_extensions | — | MIT/Apache/BSD | 常规 |
| python-dotenv | 1.2.2 | BSD | ⚠️ M5-09：notices 漏列 |

### 2.2 实际产物 `dist/_internal` 取证到、但 lock/notices 未声明的额外包

| 包 | 磁盘证据 | 源码导入 | 判定 |
|---|---|---|---|
| email_validator | `email_validator-2.3.0.dist-info` | 0 | 构建机环境污染（M5-04） |
| itsdangerous | `itsdangerous-2.2.0.dist-info` | 0 | 构建机环境污染（M5-04） |
| attrs | `attrs-26.1.0.dist-info` | 0 | 构建机环境污染（M5-04） |
| chardet | `_internal/chardet/` | 0 | 构建机环境污染；**LGPL-2.1 无 notice**（M5-09） |
| charset_normalizer | `_internal/charset_normalizer/` | 0 | 构建机环境污染（M5-04） |
| fontTools | `_internal/fontTools/` | 0（make_icon.py 用 PIL，构建期工具） | 构建机环境污染（M5-04） |
| tzdata | `_internal/tzdata/` | 0 | 构建机环境污染（M5-04） |
| win32 / pywin32 | `_internal/win32/`、`pywin32_system32/` | 0 | PyInstaller Windows hook 收集（M5-04） |

> 说明：以上额外包在干净 venv 中不复现，属「产物 ≠ lock」的可复现性证据，而非确认被应用运行时使用。

### 2.3 前端 npm 依赖（零运行时依赖）

`package.json:11-14` 仅 `eslint@^10.10.0`、`globals@^17.12.0` 两个 devDep，不进产物（M5-11）。运行时前端零 CDN、零 npm 包（V-14 闸门已验证）。

---

## 3. 交接说明（每条发现的严重性 / 状态 / 触发条件）

| 编号 | 严重性 | 状态 | 触发条件 / 复现路径 | 建议处置顺序 |
|---|---|---|---|---|
| M5-02 | S2 | 已验证 | 对比 `ci.yml:51`（审 requirements.txt）与 `ci.yml:36`（装 requirements.lock）即可见审计对象错位 | 先改审计对象，投入小、收益大 |
| M5-04 | S2 | 已验证 | 在干净 venv 重建即得与现 dist 不同闭包；当前 dist 已取证到 8 个未声明包 | 与 M5-09 一并处理（改 spec excludes） |
| M5-09 | S2 | 已验证 | 文本对账：notices 34 行 vs lock 38 项 vs dist 实际更多 | 依赖 M5-04 先剔除泄漏包再补表 |
| M5-05 | S2 | 已验证 | CI 无打包 job，check-package 恒 `[跳过] return 0` | 待「CI 出包」立项时一并补 |
| M5-01 | S2 | 已验证（理论可利用） | lock starlette 1.2.1 < 1.3；跨站利用被 `main.py:137-142` 阻断，仅本地已具权进程可 DoS | 随 fastapi/starlette 正常升版，无需紧急 |
| M5-03 | S2 | 理论 | 同版本号重打包时 lock 无哈希可辨 | 迁 pip-tools/uv 时一并做 |
| M5-06 | S2 | 理论 | action 供应链惯例加固 | 维护期顺手做 |
| M5-07 | S2 | 已验证 | 无 release workflow、spec/iss 无签名配置 | 与「CI 出包」「签名/哈希清单」打包规划 |
| M5-10 | S2 | 理论 | dependabot 合 PR 后 lock 不自动重生 | 工具链选型时定 |
| M5-13 | S2 | 理论（跨仓） | 需 med-review-site 仓确认是否复用 AGPL 渲染代码 | 产品/法务澄清 |
| M5-08 | S3 | 已验证 | README:12/:99 仍写 0.10.3 | 文档修订 |
| M5-12 | S3 | 理论 | check-package 黑名单扩充 | 下次动 datas 时一并做 |
| M5-11 | S3 | 理论 | `ci.yml:46` 去 `\|\| npm install` | 顺手 |

### 本次验证闸门实测（2026-09-17，沙箱 Python 3.12）
- `python -m ruff check .` → **All checks passed**（exit 0）
- `npm run lint`（eslint `--max-warnings 0`）→ **0 error / 0 warning**
- `python pack/check-package.py` → **[通过]**（对当前 `dist/MedKit`）
- `python -m pytest tests -q --ignore=tests/browser` → **543 passed in 94.66s**（exit 0；首次后台运行曾因资源瞬时出现收集报错，重跑全绿）
- 未执行完整打包（硬性约束）；未联网发布；未写真实 `~/.medkit/`。
- 未跑 `pip-audit` 本机复跑：本机沙箱 Python 未装该包（R7 §5.7 已记录），CVE 结论以公开 advisory + 代码版本对账给出，已逐条标注来源。

### 外部 CVE 来源（仅供 M5-01 旁证，不替代代码事实）
- starlette CVE-2026-54283（受影响 ≥0.4.1 且 <1.3.1，修复版本 **1.3.1**）：https://github.com/advisories/GHSA-82w8-qh3p-5jfq · https://nvd.nist.gov/vuln/detail/cve-2026-54283
- starlette CVE-2026-48710 BadHost（修复 1.0.1，已被 1.2.1 越过）：https://nvd.nist.gov/vuln/detail/CVE-2026-48710
- starlette CVE-2026-48817（修复 1.1.0，已被 1.2.1 越过，且本应用不用 HTTPEndpoint）：https://www.cve.org/CVERecord?id=CVE-2026-48817
- python-multipart 修复线（0.0.31/0.0.32 已覆盖历史 DoS/解析差异）：https://advisories.gitlab.com/pkg/pypi/python-multipart/

---

*审查完成。本报告为路5只读静态审查产物，未改动任何源码/配置/数据。*
