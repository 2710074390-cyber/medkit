# MedKit 审查任务书 · 路 3 · 医学内容质量与合规（领域专属）

> 本文件是《MedKit 专属审查提示词（总任务书）》的分路裁剪版，供**单路审查 Agent** 独立执行；完成后将 `问题清单_医学内容与合规.md` 交付汇总 Agent（路 6）。
> 审查对象：MedKit · 医学题库工坊（v0.10.3，仓库 `C:\Users\38063\Desktop\medkit`，HEAD 基线）
> 定位：本软件面向**即将考试的医学生**，LLM 生成题库/押题卷/复习手册/讲解/提问——**内容错了 = 学生学错 = 最高后果**
> 执行前提：先读 `README.md`、`docs/AGENT_HANDOFF.md`、`docs/adr/`（重点 ADR-003）、`docs/engineering/borrow-rules.md`、`docs/工程审查改进指南_2026-09-15.md`、`docs/验收与改进报告_2026-09-16_R7.md`；结论一律引用 HEAD 的 `file:line`。

---

## 一、任务目标

以「医学生拿着生成产物备考」为验收视角，验证**生成内容正确性防线、门禁有效性、溯源与防幻觉、医学高危内容红线、版权与考试伦理合规**，产出可复现的缺陷清单。

## 二、必读文件

- `medkit/prompts/*.md`（medgen/medqc/medfix/medreview/medexplain/medtutor/medcards/syllabus_extract 共 8 个）
- `medkit/agents/*`（medgen/medqc/medfix/medreview/medexplain/medtutor/medcards 共 7 个）
- `medkit/gates/*`（options/bloom/trace/dedup 共 4 个）
- `medkit/core/schema.py`（契约与门禁）、`medkit/core/orchestrator.py` 中门禁调用段、`medkit/core/websearch.py`（可信来源与 conflict 处理）
- `tests/fixtures/llm_cases/`、`docs/adr/ADR-003`、`docs/engineering/borrow-rules.md`

## 三、检查清单

| 编号 | 检查项 | 验证动作 |
|---|---|---|
| M3-01 | **出题正确性防线** | medgen.md 是否硬性要求「以教材为唯一事实源、禁止臆造数值/剂量/参考值、不确定即不出题」；HC 命题规则；A3/A4 案例组、B1 真组题（共享选项组）的题目-选项-答案一致性约束；模板占位符一次性替换（防教材文本二次注入）是否真实生效 |
| M3-02 | **门禁有效性（不是存在性，S0/S1 重点）** | 用反例设计验证——构造「选项重复/无唯一答案/答案与题干矛盾/溯源缺失/与教材数值冲突」的坏题，判断 `options_check`（R 规则子集）、`bloom_check`、`trace_check`、`dedup_check`（Jaccard>0.8）能否拦截；`medqc` 的 LLM-as-judge 与出题同模型——评分标准是否可能自洽放行（评分锚点、gate_decision 阈值、None/浮点 score 容错是否被滥用） |
| M3-03 | **D2 渲染前终检与人工复核闭环** | 修复轮用尽仍超限/缺字段的题是否**真被剔除出产物**而非带病渲染；`人工复核清单.md` 是否完整记录（题号/原因/处置）；**验证「网络检索 conflict 条目绝不自动改写、只进人工复核清单」声明**（websearch → 管线 → MedFix 链路上是否存在自动采纳路径） |
| M3-04 | 溯源与防幻觉 | 每题是否硬性携带 `[源:切片]`（slice 编号与教材切片实际匹配）；Web 检索引用（`[源:网 URL]`）配额 0~30% 默认 0 是否被绕过；切片与题目知识点错位风险 |
| M3-05 | **医学高危内容（S0 重点）** | 数值速查/临床路径/药物剂量/参考值类内容（复习手册、讲解、提问、记忆卡）的生成指令是否要求复核与标注「以教材/指南为准」；medexplain/medtutor 是否可能输出**直接诊疗建议**（如「你应该服用X」）而无免责声明；产品是否有医学免责声明（首启向导「体检警告」等）——缺失即发现 |
| M3-06 | **版权与来源合规（S1）** | 产物是否可能**逐字复制**用户上传的教材/教师重点/真题原文（n-gram 查重是否只查内部去重、不查与源教材的重复）；`realexams` 真题考频「不展示真题原文」声明是否属实（含前端展示、导出、讲解注入面）；押题卷/题库是否标注「AI 生成、非官方真题」；上传教材/真题的版权边界在 UI 与文档是否有说明；AGPL 再分发义务（`LICENSE`、`THIRD_PARTY_NOTICES.md`）是否完整 |
| M3-07 | 考试伦理与误导 | 押题卷文案/「押题」表述是否可能误导为官方押题；「答案明文声明」「参考价以官网为准」等声明是否存在相反实现 |
| M3-08 | 提示词治理（NX-06） | prompts/*.md 与 `tests/fixtures/llm_cases/` 契约样本是否同步（改 prompt 未改样本 = 契约漂移）；占位符校验是否覆盖全部内置提示词；影子副本「恢复默认」是否可靠 |

## 四、验证命令（只读 / 离线）

```powershell
ruff check .
python -m pytest tests -q --ignore=tests/browser
python pack/check-package.py
git status ; git log --oneline -10
```

> ⚠️ 禁止调用真实 LLM（花钱）验证门禁——用 `tests/fixtures/llm_cases/` 的既有样本 + 静态走读 + 构造契约输入做推理验证；禁止向真实 `~/.medkit/` 写入数据。

## 五、通用规则（强制）

**只读边界**：不修改任何源码/配置/数据文件；发现疑似缺陷只记录，不「顺手修复」。允许静态走读、离线测试、git 取证。

**证据规范**：每条发现必须给出 `文件:行号` + 关键代码/调用链 + 最小复现或推理步骤；明确区分「已验证可利用」与「理论风险」；医学事实/法规/考试制度类外部断言必须给出来源（指南、官方文件、教材页码）；不得用搜索结果代替代码事实。

**严重性分级**：
- S0 阻断：错误医学内容系统性流出（可能误导备考）、Key/素材/产物被窃取、付费额度被恶意耗尽
- S1 高：特定输入可稳定利用、门禁系统性绕过、版权/真题合规缺陷、关键数据丢失
- S2 中：加固不足、边界未覆盖、合规缺失（免责声明缺失等）
- S3 低：体验、文案、文档、规范性问题

**问题条目模板**（输出 `问题清单_医学内容与合规.md`）：
```
| 编号 | 标题 | 路径(file:line) | 证据/复现 | 影响 | 严重性 | 修复建议 | 状态(已验证/理论) |
```

## 六、已知加固点（只验证真实性与可绕过性，不重复上报为「新发现」）

- D2 渲染前终检：超限/缺字段题剔除出产物 + 写入人工复核清单
- 网络检索 conflict 条目「绝不自动改写」（只进人工复核清单）
- 真题考频「不展示真题原文」；押题卷「答案明文声明」
- 门禁①四类检查 + n-gram 查重（Jaccard>0.8 → MedFix 改写）；溯源回查
- 提示词契约字段以 prompts/*.md 为准（NX-06 治理）
- 检索可信来源过滤（TRUSTED_SUFFIXES/DOMAINS + 【可信】标注）

## 七、输出物与交接

1. `问题清单_医学内容与合规.md`（编号 M3-xx，按问题模板）
2. 「门禁输入 → 规则 → 反例验证结果」表（每条门禁标注：可拦截 / 可绕过 / 未覆盖）
3. 交接说明：标注每条发现的严重性、状态（已验证/理论）、触发条件，供路 6 汇总 Agent 收口。
