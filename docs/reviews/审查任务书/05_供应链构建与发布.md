# MedKit 审查任务书 · 路 5 · 供应链、构建与发布

> 本文件是《MedKit 专属审查提示词（总任务书）》的分路裁剪版，供**单路审查 Agent** 独立执行；完成后将 `问题清单_供应链构建发布.md` 交付汇总 Agent（路 6）。
> 审查对象：MedKit · 医学题库工坊（v0.10.3，仓库 `C:\Users\38063\Desktop\medkit`，HEAD 基线）
> 构建形态：PyInstaller 打包桌面应用（绿色版 ~87MB）+ Inno Setup 安装包（~38MB）；Python + 原生前端；GitHub CI
> 执行前提：先读 `README.md`、`docs/AGENT_HANDOFF.md`、`docs/adr/`（重点 ADR-007）、`docs/工程审查改进指南_2026-09-15.md`、`docs/验收与改进报告_2026-09-16_R7.md`；结论一律引用 HEAD 的 `file:line`。

---

## 一、任务目标

以「用户拿到的安装包/绿色版/依赖树」为审查对象，找出**依赖漏洞、打包纯净性、CI/CD 安全、AGPL 合规、更新链路**问题，产出可复现的缺陷清单。

## 二、必读文件

- `requirements.txt` / `requirements-dev.txt` / `requirements.lock`、`package.json` / `package-lock.json` / `eslint.config.js`、`pyproject.toml`
- `medkit.spec`、`medkit.iss`、`medkit/__init__.py`（版本单源）
- `pack/*`：build.bat、check-package.py、recover-user-data.py、make_icon.py
- `.github/workflows/ci.yml`、`.github/dependabot.yml`
- `LICENSE`、`THIRD_PARTY_NOTICES.md`、`CHANGELOG.md`（版本与 Prompts 小节）

## 三、检查清单

| 编号 | 检查项 | 验证动作 |
|---|---|---|
| M5-01 | **依赖漏洞** | Python 依赖（重点 FastAPI / uvicorn / pydantic / jieba / py-fsrs / requests）与 npm 依赖的已知 CVE；`requirements.lock` 是否真锁定可复现；PyInstaller 打包内嵌依赖版本可溯源性 |
| M5-02 | **打包与纯净性** | `pack/check-package.py` 断言（无样例/种子/测试/字节码）覆盖哪些路径、能否被绕过（`data/`、`docs/`、`tests/` 混入产物）；`medkit.iss` 安装脚本的目录与权限设置；版本单源（`__init__.py → build.bat → version.iss`）一致性 |
| M5-03 | **CI/CD 安全** | `ci.yml` 中 GitHub Actions 版本是否 pin 完整 SHA、secrets 是否在 PR 触发工作流中暴露、上传 artifact 是否含敏感文件（.coverage、日志、Key 掩码样本）；`dependabot.yml` 配置 |
| M5-04 | **许可合规** | AGPL-3.0 完整性；`THIRD_PARTY_NOTICES.md` 是否覆盖全部运行时第三方（py-fsrs、jieba、MinerU 通道、pyinstaller 及内嵌）；ADR-007 决策是否与 `LICENSE` 一致；与在线站（med-review-site）同源同规格声明是否合规 |
| M5-05 | **更新与反馈链路** | `core/update.py` 版本比较（pre-release 后缀处理）、GitHub Releases 下载 URL 构造（供应链误导/重定向风险）；邮件反馈 `mailto:` 附带的版本/系统信息是否过度 |

## 四、验证命令（只读 / 离线）

```powershell
ruff check .
npm run lint
python -m pytest tests -q --ignore=tests/browser
python pack/check-package.py
git status ; git log --oneline -10
```

> ⚠️ 禁止执行完整打包（耗时长且可能触网）；纯净性验证用 `pack/check-package.py` 只读断言 + 对 `dist/` 旧产物做静态检查。禁止调用真实 API。

## 五、通用规则（强制）

**只读边界**：不修改任何源码/配置/数据文件；发现疑似缺陷只记录，不「顺手修复」。允许静态走读、离线测试、git 取证。

**证据规范**：每条发现必须给出 `文件:行号` + 关键代码/调用链 + 最小复现或推理步骤；明确区分「已验证可利用」与「理论风险」；CVE/许可条款类外部断言给出来源链接；不得用搜索结果代替代码事实。

**严重性分级**：
- S0 阻断：Key/素材/产物被窃取、恶意依赖进入产物、付费额度被恶意耗尽
- S1 高：特定输入可稳定利用、许可/发布合规缺陷、CI 密钥泄露
- S2 中：加固不足、边界未覆盖、依赖维护欠账、可靠性风险
- S3 低：体验、文案、文档、规范性问题

**问题条目模板**（输出 `问题清单_供应链构建发布.md`）：
```
| 编号 | 标题 | 路径(file:line) | 证据/复现 | 影响 | 严重性 | 修复建议 | 状态(已验证/理论) |
```

## 六、已知加固点（只验证真实性与可绕过性，不重复上报为「新发现」）

- 纯净包检查（pack/check-package.py）自动运行；npm lint 入 CI（`--max-warnings 0`）
- 版本单源 `medkit/__init__.py`；依赖国内镜像安装说明
- AGPL-3.0 + THIRD_PARTY_NOTICES 已声明；ADR-007 决策记录
- dependabot 已配置；更新检查仅提醒 + 跳转下载页

## 七、输出物与交接

1. `问题清单_供应链构建发布.md`（编号 M5-xx，按问题模板）
2. SBOM 快照（运行依赖清单 + 版本 + 风险标注）
3. 交接说明：标注每条发现的严重性、状态（已验证/理论）、触发条件，供路 6 汇总 Agent 收口。
