# MedKit 仓库事故与恢复记录（2026-09-15）

> **性质**：`.git` 目录被整体删除（后从回收站恢复）导致的**对象库损坏** + `master` 被重置，
> 造成 R5 批次 0/1/2 的提交链损坏。**代码内容无损失**，仅 git 历史受损。
> **处置人**：R6 审查会话。**处置原则**：只读取证 → 备份 → 保全幸存对象 → 重建提交 → 立即推送。
> 全过程**未执行** `git gc` / `prune` / `repack`（这三者会清除仅存的悬空对象，断掉恢复可能）。

---

## 1. 事实（全部命令实测）

| 时间 | 观测 | 证据 |
|---|---|---|
| 11:02 | `HEAD = 55c4225`（R5 批次 2），领先 `origin/master` **3 个提交**，工作区 26 文件改动 | `git status` / `git log` |
| 11:29 | `HEAD = 9a9058a`（v0.10.1，**与 origin/master 相同**），领先 0 个提交，工作区 **37 文件**改动 | `git status` / `git log` |

- `git fsck`：`missing tree 072ddf6…` + `broken link from commit 8f98d44… to tree 072ddf6…`
  → **R5 批次 0 提交的根树丢失**，该提交不可检出；另有 `broken link from tree 5cfbcadb… to blob e6e3e587…`。
- `error: HEAD: invalid reflog entry 55c4225…` / `refs/heads/master: invalid reflog entry 55c4225…`
- `git cat-file -t 55c4225` → `fatal: Not a valid object name` → **R5 批次 2 提交对象已不存在**。
- 对象库内提交总数 89；悬空提交中含 `7a8e9bd`（2026-09-15）「`untracked files on master: 55c4225 …`」
  —— 即有人今日执行过 `git stash -u`，但 **stash 主提交已丢失**（`refs/stash` 随 `.git` 一并损毁）。

## 2. 内容损失评估：无

| 内容 | 状态 |
|---|---|
| R5 批次 0 + 批次 1（含 `tests/conftest.py` 隔离、`pack/recover-user-data.py`、`dedupe.py`、`llm.py`、`routers/library.py`） | ✅ **完整**——`eb7855a` 提交对象与其树（222 文件）完好，相对 `9a9058a` 仅 7 文件差异 |
| R5 批次 2（模板键绑 pid / assets 累计配额 / 删除复核 / 文档勘误） | ✅ 内容完整存在于工作区；提交信息完整存在于 `.git/COMMIT_EDITMSG` |
| 其余未提交工作（30 文件） | ✅ 完整存在于工作区 |
| `dist-installer/` 0.10.1 产物 | ✅ 文件在位，指纹见备份目录 `_manifest_installers.sha256` |

**唯一不可恢复的**：`55c4225` 与 `8f98d44` 两个提交对象本身（历史结构），以及 `refs/stash`。

## 3. 处置动作（按序）

1. **备份**
   - `.git` 全量 → `%TEMP%\medkit-git-backup-20260915-113106\git`（14 MB）+ `HEAD.sha` + `status.txt`
   - 工作区源码与文档（排除 `dist/`、`build/`、`dist-installer/`、缓存）→
     `%TEMP%\medkit-worktree-backup-20260915-115017\`（20 MB）+ `_manifest_head.txt` / `_manifest_status.txt` / `_manifest_installers.sha256`
2. **保全幸存提交**：`git branch r5-batch0-1 eb7855a`
   —— 把唯一完整的 R5 提交挂到真实引用上，避免被后续任何清理动作当作垃圾回收。
3. **导出悬空对象**：`git fsck --lost-found` → `.git/lost-found/{commit,other}`（6 个悬空提交 + 若干 blob/tag）。
4. **重建提交**（见 §4）。
5. **立即推送**（见 §4 第 3 条）。

## 4. 重建的提交结构

```
9a9058a (origin/master, v0.10.1)
  └─ A  recover(r5): 重建 R5 批次 2 与其余未提交工作
       └─ B  fix(test): R6-01 总闸转绿（闸门对齐 + 协程线程化 + ruff 清零）

r5-batch0-1 → eb7855a  (R5 批次 1，含批次 0 内容；独立引用保全，不参与主线)
```

- **为何批次 0/1 不再重建**：`eb7855a` 完好且已由 `r5-batch0-1` 引用保全；在其祖先 `8f98d44`
  （根树缺失）之上重建会让主线带上一个损坏对象，故主线回到已知完好的 `9a9058a` 之后线性重建。
- **提交 A / B 的分界**：B 只含 R6-01 独有的文件（`verify.cmd`、`.github/workflows/ci.yml`、
  `tests/browser/conftest.py`、`tests/test_explain_stream.py`、`tests/test_tutor_stream.py`、
  `tests/conftest.py`、`docs/工程审查改进指南_2026-09-15.md`）；其余（含被两侧共同修改的
  `medkit/routers/ocr.py`、`medkit/routers/projects.py`、`tests/test_cards.py`、`tests/test_wp04.py`、
  `tests/test_r4_batch3.py`）归入 A。
- **未纳入提交**：`docs/reviews/工程质量独立核查报告_2026-09-15.md` —— 属**另一并发会话**的在写产物，
  不在本次恢复范围内，保持未跟踪状态，由该会话自行提交。

## 5. 遗留风险与后续要求

1. ⚠️ **`git gc` / `git prune` / `git repack -a -d` 仍须避免**，直到 `r5-batch0-1` 分支与
   `.git/lost-found/` 已完成异地归档（`git fsck` 仍会报 `missing tree 072ddf6`，因为 `eb7855a`
   的祖先 `8f98d44` 树缺失——这是已知且不可修复的残留）。
2. ⚠️ **本仓库存在多会话并发写入**（本次事故期间另一会话在写 `docs/reviews/`、`.workbuddy-ai/uxwalk/`，
   且会写真实 `~/.medkit`，导致 R5-01 防污染哨兵在测试 teardown 误报）。动手前先核对 `git log -1`。
3. ✅ **提交后立即 push**：未推送的提交只存在于本地对象库，是本次风险敞口的主因（R5 三次「改完不推」）。
4. 建议把 §5 第 1/3 条并入 `docs/AGENT_HANDOFF.md` §4 陷阱清单。

---

*记录人：R6 审查会话 · 2026-09-15。本文件本身随恢复提交 A 一并入库，作为该段历史的可追溯凭据。*
