# MedKit 审查任务书・路 1・前端与产物渲染安全

> 本文件是《MedKit 专属审查提示词（总任务书）》的分路裁剪版，供
>
> **单路审查 Agent**
>
>  独立执行；完成后将 
>
> `问题清单_前端与产物渲染.md`
>
>  交付汇总 Agent（路 6）。
> 审查对象：MedKit・医学题库工坊（v0.10.3，仓库 
>
> `C:\Users\38063\Desktop\medkit`
>
> ，HEAD 基线）
> 运行形态：FastAPI 本地回环服务 + 零构建原生前端；题库 / 押题卷 / 复习手册 HTML 与 Anki 包由 
>
> `medkit/render/*`
>
>  生成
> 执行前提：先读 
>
> `README.md`
>
> 、
>
> `docs/AGENT_HANDOFF.md`
>
> 、
>
> `docs/adr/`
>
> （ADR-001/003/005/006/007）、
>
> `docs/工程审查改进指南_2026-09-15.md`
>
> 、
>
> `docs/验收与改进报告_2026-09-16_R7.md`
>
> 、
>
> `docs/reviews/`
>
>  历史报告 —— 避免把已修复 / 已验收项当作新发现重复上报；结论一律引用 HEAD 的 
>
> `file:line`
>
> ，不得引用旧产物（
>
> `build-old-*`
>
> /
>
> `dist-old-*`
>
> ）。



***

## 一、任务目标

以「医学生打开生成的题库 / 押题卷 / 复习手册页面」为第一视角，找出产物与前端中的**注入面、可被篡改的本地存储逻辑、前端安全防线绕过**，产出可复现、可修复的缺陷清单。

## 二、必读文件



* `medkit/web/index.html`、`medkit/web/js/*`（含 V-15 分片：learn/learn-study/learn-live/learn-review、review-desk 系列）

* `medkit/render/qbank_html.py`（题库 HTML，59KB）、`medkit/render/review_html.py`（复习手册）、`medkit/render/pagechrome.py`（三套产物主题单源）、`medkit/render/apkg.py`

* `medkit/core/cards.py`（Anki 卡数据面）

* `tests/browser/*`、`tests/test_v15_frontend_split.py`、`tests/test_render_markdown.py`

## 三、检查清单



| 编号    | 检查项                                                                                                               | 验证动作                                                                                                                                            |
| ----- | ----------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- |
| M1-01 | **产物 HTML 注入面（S0/S1 重点）**：题库 / 押题卷 / 复习手册渲染是否对题干、选项、解析、溯源（`[源:切片]`/`[源:网 URL]`）、章节名、教师重点条目等**全部用户 / LLM 可控字段**做转义 | 逐字段枚举「可控来源 → 渲染出口 → 消毒手段」，构造含 `<script>`/ 事件属性 / SVG / 实体编码的输入走读渲染链                                                                             |
| M1-02 | 复习手册白名单消毒（href 仅 http/https）的绕过路径                                                                                 | 验证 `data:`、`javascript:`（大小写 / HTML 实体 /`&#x6a;` 编码）、`vbscript:`、协议相对 `//`、`<img src=x onerror=…>`、SVG `onload`、事件属性、Markdown 链接 / 图片注入、表格单元格注入 |
| M1-03 | `md.js`「先转义再解析」的顺序是否可被构造输入破坏                                                                                      | 走读 `mdRender`/`mdHighlight` 实现，找转义后二次解析引入注入的路径                                                                                                  |
| M1-04 | **押题卷交互与本地存储**：判分 / 答题卡 / 续答（localStorage）逻辑                                                                      | 验证 localStorage 数据是否被信任并渲染进 DOM（恶意 localStorage → XSS 链）；X 型集合判分是否存在篡改得分绕过；错题重练写学习中心接口是否校验来源                                                    |
| M1-05 | 前端鉴权与成本面                                                                                                          | token/Key 在前端的存在形态与掩码（前 2 后 2）是否所有回显点一致；成本预估（前端粗估「参考价」）是否可被脚本篡改误导；`api()` 封装与自定义端点 `base_url` 客户端校验是否可绕过（指向内网 / 非法 scheme）                      |
| M1-06 | 经典脚本全局作用域与 DOM clobbering                                                                                         | V-15 分片加载顺序契约与 `/* exported */` 声明；`window.X`/`global.X` 挂载是否可被第三方注入或产物 HTML 覆盖                                                                 |
| M1-07 | CSP 与安全头                                                                                                          | 本地服务响应与产物 HTML 是否设置 CSP / 安全头；零 CDN 约束下内联脚本的实际风险面                                                                                               |
| M1-08 | 下载与导出面                                                                                                            | `project_file`/`export_anki` 的下载路径与文件名处理；Anki `.apkg` 生成（zip 结构）是否存在 zip slip / 解压炸弹；产物单文件内嵌 base64 图片（WP-04 `<figure>`）的尺寸上限是否失控               |

## 四、验证命令（只读 / 离线）



```
ruff check .

npm run lint                          # 前端 ESLint，--max-warnings 0

python -m pytest tests -q --ignore=tests/browser

python pack/check-package.py

git status ; git log --oneline -10
```

> ⚠️ 禁止运行消费性动作（真实 LLM/OCR/ 检索、run_project、parse、ocr_start），禁止向真实 
>
> `~/.medkit/`
>
>  写入数据。

## 五、通用规则（强制）

**只读边界**：不修改任何源码 / 配置 / 数据文件；发现疑似缺陷只记录，不「顺手修复」。允许静态走读、离线测试、git 取证；确需启动服务观察时使用 `MEDKIT_PORT=4999 MEDKIT_NO_BROWSER=1` 且不指向真实 HOME。

**证据规范**：每条发现必须给出 `文件:行号` + 关键代码 / 调用链 + 最小复现或推理步骤；明确区分「已验证可利用」与「理论风险」；外部事实给出来源链接，不得用搜索结果代替代码事实；同一问题跨多处出现时报告所有位置。

**严重性分级**：



* S0 阻断：Key / 素材 / 产物被窃取、付费额度被恶意耗尽、错误医学内容系统性流出

* S1 高：特定输入可稳定利用（XSS / 注入 / 路径穿越 / 越权读产物）、门禁系统性绕过、关键数据丢失

* S2 中：加固不足、边界未覆盖、竞态 / 可靠性风险、合规缺失

* S3 低：体验、文案、文档、规范性问题

**问题条目模板**（输出 `问题清单_前端与产物渲染.md`）：



```
\| 编号 | 标题 | 路径(file:line) | 证据/复现 | 影响 | 严重性 | 修复建议 | 状态(已验证/理论) |
```

## 六、已知加固点（只验证真实性与可绕过性，不重复上报为「新发现」）



* 产物 HTML 全量转义 + 复习手册 href 白名单（仅 http/https）+ `javascript:` 剥离 + md.js 先转义再解析

* 押题卷答案明文声明；判分后锁定；成本预估前置（参考价标注）

* 零 CDN、经典脚本分片加载顺序契约（tests/test\_v15\_frontend\_split.py 为闸门）

* 全局 onerror/unhandledrejection 兜底；390px 窄屏溢出修复

## 七、输出物与交接



1. `问题清单_前端与产物渲染.md`（编号 M1-xx，按问题模板）

2. 「用户可控字段 → 渲染出口 → 消毒手段」对照表（覆盖度一眼可见，注明每个出口是否已消毒）

3. 交接说明：标注每条发现的严重性、状态（已验证 / 理论）、触发条件，供路 6 汇总 Agent 收口。