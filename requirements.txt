# Runtime dependency intent declaration (U-16 / S2-23).
# NOTE: versions MUST stay ==-pinned and identical to requirements.lock one-by-one.
# Floating ranges drift to newer closures (e.g. openai 3.x pulls in
# httpcore2/httpx2/truststore) and break the S2-23 closure-consistency gate in CI
# (ubuntu installs from this file, then derives the closure from installed metadata
# and compares it with the lock). Upgrade flow: edit here -> regenerate the lock +
# sync THIRD_PARTY_NOTICES.md -> run full local verification. The lock (--hash) stays
# the single source of truth for reproducible builds.
# Keep all comments ASCII-only: pip_requirements_parser auto-decodes with the locale
# codec (cp1252 on Windows) and crashes on non-ASCII bytes (see b360f90).
fastapi==0.136.3
uvicorn[standard]==0.49.0
openai==2.41.0
pymupdf==1.27.2.3
python-docx==1.2.0
httpx==0.28.1
python-multipart==0.0.32
markdown==3.10.2
genanki==0.13.1
jieba==0.42.1   # IMP-06: slices_fts FTS5 word-split (K1 verified; D2/ADR-002)
fsrs==6.3.2     # NX-04/WP-05: memory-card FSRS scheduler (K2 verified; default algo; SM-2 legacy switchable)
