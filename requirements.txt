# 运行时依赖「意图声明」（U-16 / S2-23）。
# 注意：**全部版本必须与 requirements.lock 逐条 == 一致**——浮动区间会随 pip 解析
# 漂移到新一代闭包（如 openai 3.x 引入 httpcore2/httpx2/truststore），使
# tests/test_lock_closure.py 的 S2-23 闭包一致性闸门失败（CI ubuntu 按本文件安装，
# 用当前环境元数据推导闭包与 lock 比对）。升级流程：改这里 → 重生成 lock + 同步
# THIRD_PARTY_NOTICES.md → 本地全量验证。lock 仍为复现构建（--hash 校验）唯一来源。
fastapi==0.136.3
uvicorn[standard]==0.49.0
openai==2.41.0
pymupdf==1.27.2.3
python-docx==1.2.0
httpx==0.28.1
python-multipart==0.0.32
markdown==3.10.2
genanki==0.13.1
jieba==0.42.1   # IMP-06: slices_fts FTS5 word-split (K1 verified; D2/ADR-002)
fsrs==6.3.2     # NX-04/WP-05: memory-card FSRS scheduler (K2 verified; default algo; SM-2 legacy switchable)
