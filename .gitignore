__pycache__/
*.pyc
.venv/
venv/
.medkit/
config.json
build/
dist/
dist-installer/
# 旧构建目录重命名（如出包留档 dist-old-0.10.1-pre-r5 / build-old-0.10.1）——
# 用通配以防 `git add -A` 把重命名后的构建产物误提交（2026-09-15 曾误提交 843 个文件）
dist-old*/
build-old*/
# 2026-09-17 整理：旧构建快照统一归档到 archive/（builds/ + dist/），
# 只保留归档说明 README.md 入库（否则「为什么有 archive/」这件事会失传）。
# 根目录因此从 27 个条目收敛到 12 个。
archive/*
!archive/README.md
pack/icon_preview.png
projects/
*.spec.bak
.ruff_cache/
.pytest_cache/
*.bak
.DS_Store

.vscode/
.idea/
htmlcov/
*.log
# V-06：覆盖率数据文件（二进制）——此前被入库，每次跑测试都脏工作区
.coverage
.coverage.*

# U-17：前端静态检查（ESLint）的依赖目录——仅开发期使用，不入库
node_modules/

docs/spikes/k4_out/

.workbuddy-ai/
.review-run/
